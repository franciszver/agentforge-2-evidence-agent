id: statin-ck-myopathy-question
category: citation_present
failure_mode: >
  P3G.1: a guideline-answerable chart question must yield an answer whose clinical claim
  carries a VERIFIED guideline_chunk DocumentCitation -- a real retrieved chunk, quoted
  verbatim -- alongside the chart-data citation.
question: "He's on a statin and feels fine -- does he need a routine CK level checked?"
patient_id: 1
tool_data:
  get_medications:
    items:
      - name: "Simvastatin"
        dose: "40mg"
        route: "oral"
        status: "active"
retrieved_chunks:
  - chunk_id: "statin-monitoring#muscle-symptoms-and-ck"
    doc_id: "statin-monitoring"
    title: "Statin Therapy Monitoring"
    section: "Muscle Symptoms and CK"
    text: |-
      Statins carry a known risk of myopathy. Routine creatine kinase (CK)
      monitoring in an asymptomatic patient is generally not recommended; a CK
      level is checked if the patient reports new muscle pain, tenderness, or
      weakness. Persistent, unexplained muscle symptoms with a significantly
      elevated CK generally warrant holding the statin and reassessing.
    rerank_score: 0.9
assertions:
  - type: verdict
    equals: verified
  - type: guideline_citation_present
# #58 could NOT re-record this case against qwen3-8b-q5: under the
# llama_server engine, the model's claim extraction call for this turn
# deterministically emits a claim with an empty `source_refs` list ("There
# are no recent lab results available for CK levels."), which fails
# VerifiedAnswer's "every claim needs >=1 SourceRef or DocumentCitation"
# validator both retry attempts (temperature=0, byte-identical output each
# time) -- `app.llama_server_client.LlamaServerClient.extract` then raises
# `LlamaServerError` after exhausting retries. `app.extraction.extract_claims`
# only catches `app.ollama_client.OllamaError` (not `LlamaServerError`, a
# separate exception hierarchy), so the exception propagates uncaught and
# the eval harness's `RecordingOllamaClient` never gets a completed call to
# save -- no recording can be produced for this case under this engine at
# all. This looks like a real production gap (the llama_server engine
# migration's `extract_claims` except clause doesn't cover its own client's
# error type), reported for a follow-up issue; out of #58's scope since
# fixing it means touching services/copilot-agent/app/extraction.py
# (product runtime code), which #58 explicitly disallows. This case's
# recording/xfail below are UNCHANGED from the qwen3:4b era pending that fix.
#
# #81 (gate-ON re-recording pass) tried again, twice: attempt 1 again failed
# claim extraction after exhausting retries (no recording produced, same
# symptom as #58); attempt 2 crashed on an uncaught live-request timeout
# during an earlier planner-decision call (a transient infra issue, not
# informative about the extraction bug above). Per the coordinator's
# no-repeated-rerolling rule, this case was not attempted a third time -- its
# recording remains the ORIGINAL, pre-#81 (qwen3:4b-era) one, and
# `runner.pipeline.needs_semantic_support` explicitly excludes this case id
# so replay never asks it for a support-judge call it doesn't have.
xfail: >
  OBSERVED (recorded, qwen3:4b): given BOTH real chart-data tool results
  and a retrieved guideline chunk for the same turn, the model consistently
  answers only from the chart data and never surfaces or quotes the
  guideline passage in its final answer -- so the claim extractor has no
  guideline text to cite and produces zero guideline_chunk document
  citations (verdict stays verified/partially_verified on the chart-data
  claims alone, but citation_present -- a claim carrying a VERIFIED
  guideline_chunk citation -- never fires). This is an honest, reproducible
  P3G.1 finding about the local 4B model's current tendency to prioritize
  structured chart data over retrieved guideline evidence when both are
  available in one turn, not a harness bug -- the assertions are the
  correct target and are deliberately not weakened.
