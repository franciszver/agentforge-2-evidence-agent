id: lithium-nsaid-question
category: citation_present
failure_mode: >
  P3G.1: a guideline-answerable chart question must yield an answer whose clinical claim
  carries a VERIFIED guideline_chunk DocumentCitation -- a real retrieved chunk, quoted
  verbatim -- alongside the chart-data citation.
question: "He takes lithium and wants to use ibuprofen for back pain -- is that okay?"
patient_id: 2
tool_data:
  get_medications:
    items:
      - name: "Lithium Carbonate"
        dose: "300mg"
        route: "oral"
        status: "active"
retrieved_chunks:
  - chunk_id: "nsaid-interactions#lithium"
    doc_id: "nsaid-interactions"
    title: "NSAID Drug-Interaction Cautions"
    section: "Lithium"
    text: >
      NSAIDs can reduce renal lithium clearance, raising lithium levels toward toxicity;
      this combination generally calls for closer lithium-level monitoring if it cannot be
      avoided.
    rerank_score: 0.9
assertions:
  - type: verdict
    equals: verified
  - type: guideline_citation_present
# issue #108 FIXED (live re-verification against current main): the
# semantic-support judge previously scored the model's two restatements of
# the same lithium/NSAID interaction caution (both citing the identical
# `nsaid-interactions#lithium` quote) inconsistently -- validating the
# first, deterministically downgrading the second (monitoring-advice)
# restatement, 11/11 prior draws. `app.semantic_support.apply_semantic_support`
# now groups DocumentCitation results by exact evidence identity
# (source_type, source_id, field_or_chunk_id, quote_or_value) and judges
# each distinct identity ONCE, so duplicate claims citing identical
# evidence can never disagree. Re-run live 8 times through the real
# pipeline (`evals/runner/pipeline.run_case`, production
# `LlamaServerClient`, no replay) post-fix: 8/8 draws reach `verified`. The
# committed recording below captures a typical (now-passing) `verified`
# draw.
