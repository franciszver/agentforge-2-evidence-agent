id: fixture-pass
category: tool_selection
failure_mode: >
  Harness self-test: proves the runner reports PASS when a case's recording
  replays through the real pipeline and every assertion holds.
question: "What meds is she on?"
patient_id: 1
tool_data:
  get_medications:
    items:
      - name: Lisinopril
        dose: 10mg
        route: oral
        status: active
assertions:
  - type: first_tool_in
    tools: [get_medications]
  - type: answer_contains
    phrases: ["Lisinopril"]
