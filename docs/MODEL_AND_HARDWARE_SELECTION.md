# Clinical Co-Pilot — Model & Hardware Selection (Phase 2 Evidence Agent)

- **Status:** Final for Phase 2's evidence-retrieval answer path. Documents how the local answer model and its serving configuration were chosen for the reference/minimum hardware tier, and why the verified-citation ceiling that choice produces is honestly **low** on this hardware tier — not a defect to be tuned away, but the capability boundary of an 8 GB GPU at this model class, now measured against a **tightened** definition of "verified" (see immediately below).
- **Live re-verification (issue #100):** the citation_present baseline stated throughout most of this document as "5/12" was measured by re-judging one frozen, historical answer recording per case, and that recording was never checked against what a fresh live draw actually produces today. It has now been checked: none of the 5 non-xfail cases reliably reproduce `verified` on a fresh live draw (1 pass in 42 draws total). The honest, currently live-reproducible number was **0/12** — see "Live re-verification (issue #100)" below for the full method and data before trusting any "5/12" reference elsewhere in this document as current. **Issue #108's fix (below, "Issue #108 follow-up") moved this number to 2/12**, **issue #116's stale-recording refresh (below, "Issue #116 follow-up") moved it to 3/12** — `warfarin-antibiotic-question` now reliably reaches `verified` live with a real guideline citation; `metformin-renal-monitoring-question` and `statin-liver-monitoring-question` were both checked at a larger sample and remain honestly `xfail`, each for a distinct, precisely-documented reason — **issue #114's larger live sample (below, "Issue #114 findings") has since moved it to 4/12**, un-xfailing `statin-ck-myopathy-question` after a 25-draw live sample found its original chart-data-only pattern no longer reproduces (92% verified) — **issues #111/#128's combined judge-context-scoping fix (below, "Issue #111/#128 findings") has since moved it to 6/12**, un-xfailing both `bp-stage2-question` and `lipid-panel-ldl-question` — and **issue #133's live re-verification (below, "Issue #133 findings") has since moved it to 7/12**, un-xfailing `nsaid-ace-inhibitor-question` after its old stale-recording rationale did not reproduce (`renal-function-ace-question` was checked at the same rigor and remains honestly `xfail` for a distinct, freshly-documented reason).
- **"Verified" tightened (issue #47/#81):** earlier revisions of this document measured verification as provenance only — a citation counted once its quote was confirmed a real, verbatim substring of the source. That leaves a gap: a model can pair a real quote with a claim the quote doesn't actually support. Issue #47 named this gap; issue #81 shipped the fix (`app/semantic_support.py`) ON by default and re-measured through it. **A citation now counts only when BOTH hold: the quote is verbatim-real (provenance) AND an LLM judge (the same Qwen3-8B-Q5 engine) affirms the quote actually supports the claim's prose (semantic support).** Every count below uses this tightened definition unless explicitly marked otherwise.
- **Method — two different measurements, not one, stated plainly so neither is mistaken for the other:**
  1. **The exploratory hardware-selection sweep** (below, "The ladder actually tested"): a throwaway measurement harness run once per config, best-of-attempts, used only to choose *which* model/config to adopt. Its 8B-Q5 row (4/12) predates the semantic-support gate entirely (provenance-only) and is kept here only as the historical selection rationale.
  2. **The committed, CI-reproducible production baseline** (below, "The committed production baseline"): the chosen 8B-Q5 config's citation reliability as measured through the REAL eval pipeline (`evals/cases/citation_present/`) — production `LlamaServerClient`, the gate's own effect isolated by re-judging the STABLE, already-committed answer recordings (rather than re-drawing fresh answers) with a live semantic-support judge call, fail-closed verification (gate ON) — the number `pytest evals/` reproduces on every run and the P3G.2 gate now guards.
- **Run-to-run variance is real and disclosed, not folded into the baseline.** A full fresh re-draw of the answer pipeline (planner + extraction, not just the judge) is noisy on this 8B model: repeated from-scratch re-recording attempts of all 12 cases have produced results ranging from 1/12 to 6/12 provenance-passing answers across draws, purely from planner/extraction variance unrelated to the semantic-support gate. Because of that, the committed production baseline below deliberately measures the gate's effect in isolation — by re-judging the stable, already-committed recordings rather than re-drawing new answers — so the published number is a property of one fixed, committed set of answers, not a lucky or unlucky sample of the answer pipeline itself. The variance is real and is disclosed here rather than smoothed over.
- **Related:** `docs/W2_ARCHITECTURE.md` §"Reference Hardware & Model Tiers" (the same minimum-vs-recommended framing, applied there to the VLM/ingestion path) and §"Inference Engines: the Final Partial-Consolidation Architecture" (this model is what llama.cpp serves for answer/extract/rerank, alongside embeddings — vision stays on Ollama); `docs/ARCHITECTURE.md` §"Capacity reality" (the Phase 1 precedent for measuring on real hardware before publishing a spec).

## Reference (minimum) hardware

**RTX 5060 Laptop GPU, 8 GB VRAM; 32 GB system RAM.**

This is the same dev/demo hardware Phase 1 measured its chat-latency and concurrency ceilings on (`docs/ARCHITECTURE.md`). Phase 2 keeps that precedent: the published minimum spec is whatever was actually measured on this box, not an estimate. If a configuration doesn't fit or doesn't perform acceptably here, it isn't the minimum spec — it's the recommended-tier story instead (see below).

## The ladder actually tested (exploratory hardware-selection sweep)

Four model/config combinations were run end-to-end against the same 12 eval cases, using a throwaway measurement harness (best-of-attempts, not the committed eval suite) whose sole purpose was choosing which model/config to adopt for this hardware tier:

| Model / config | Engine | Verified citations | Latency | Hardware fit |
|---|---|---|---|---|
| Qwen3-4B @ 16k ctx | Ollama | 1/12 | ~22 s | Fits GPU |
| Qwen3-8B-Q4_K_M @ 16k ctx, q8_0 KV, flash-attn | llama.cpp | 3/12 | ~23 s | Fully GPU-resident, 6.27 GB / 8 GB |
| **Qwen3-8B-Q5_K_M @ 16k ctx, q8_0 KV, flash-attn (chosen)** | llama.cpp | **4/12** (exploratory sweep) | ~27 s | Fully GPU-resident, 6.63 GB / 8 GB |
| Qwen3-30B-A3B (MoE, Q4) @ 16k ctx, q8_0 KV, expert-offload (`--cpu-moe`) | llama.cpp | 0/12* | median 152 s, max 300 s (timeout) | Experts in CPU RAM; only 1.9 GB VRAM |

The chosen row's exploratory figure (4/12) is a **provenance-only** measurement from before the semantic-support gate existed, and is superseded by the committed, CI-reproducible production baseline below, which now measures provenance AND semantic support together. The two numbers aren't contradictory: the sweep was a quick best-of-attempts comparison used only to rank the four candidates against each other, and it correctly ranked 8B-Q5 above 8B-Q4 and 4B; the production baseline is the number this project actually publishes and enforces going forward, under the tightened definition.

*The 30B-A3B row is a confound, not a citation verdict, and is called out explicitly rather than left to imply the model can't cite at all: 10 of 12 cases failed with extraction timeouts before completion — the verbose reasoner exhausted its generation budget on internal reasoning before ever emitting the constrained JSON output the verifier needs, so only 2 of 12 cases ran to completion. It is disqualified primarily on **latency**, not citation quality: 3–12× slower than the 8B, because a 30B model's experts, offloaded to CPU RAM on an 8 GB card, are memory-bandwidth-bound (roughly 36 tokens/s). No serving configuration fixes this at this hardware tier — the bottleneck is CPU↔GPU memory bandwidth, not a tunable parameter.

## The committed production baseline (superseded by issue #100 — see below)

Once 8B-Q5 was selected, its real citation reliability was measured the way every other eval category in this project is measured: the 12 `citation_present` eval cases (`evals/cases/citation_present/`) run through the actual production pipeline (planner → claim extraction → fail-closed provenance re-validation → the issue #47 semantic-support judge), against the production `LlamaServerClient` for every one of those roles. To isolate the gate's own effect from the answer-pipeline variance described above, issue #81's methodology re-judges the STABLE, already-committed answer recordings (the 6 that already passed provenance-only re-validation) rather than re-drawing fresh answers: for each, the recorded planner/extraction calls replay unchanged, and a live semantic-support judge call is made and appended to that same recording — so the full gate-ON pipeline still replays byte-for-byte offline in CI, but the number reflects the gate's effect on a fixed, known answer set rather than a fresh draw's luck.

**This methodology's blind spot, found by issue #100: a "stable" recording is only as good as the draw that produced it, and that draw is never re-checked against what the live model actually does today.** The section immediately below documents what happened when it finally was checked. The historical result is kept here for provenance, but it no longer describes current live reality — see "Live re-verification (issue #100)" for the number this project now stands behind.

**Historical result (superseded): 5 of 12 cases genuinely passed under the tightened definition, as of the #81 re-judge.** Of the 6 cases that passed provenance-only re-validation on the stable, committed recordings, 5 were judge-confirmed semantically supported and stayed non-`xfail`: `bp-stage2-question`, `a1c-target-question`, `dual-antiplatelet-question`, `hypertension-lifestyle-followup-question`, `lithium-nsaid-question`. The 6th was honestly downgraded:

- **`statin-liver-monitoring-question` — the case issue #47/#81 specifically targeted — was downgraded to `xfail`.** Its committed recording carries exactly the provenance-without-support defect issue #47 was opened over: a real, verbatim quote ("routine ongoing liver-enzyme monitoring ... is usually not needed") paired with claims asserting the opposite (monitoring "is typically recommended" / the clinician "should review the specific statin"). The live semantic-support judge correctly called **both affected claims `not_supported`**, downgrading the verdict to `partially_verified`. This was **not** counted as a 6th supported citation — the gate did exactly what it was built to do, on the exact case it was built to catch. This part of the finding is unaffected by issue #100 and still stands.
- The other **6 cases remained documented `xfail`** for reasons unrelated to the gate — the same chart-data-only / extraction-decode patterns this document has tracked since the exploratory sweep and #58 (`lipid-panel-ldl-question`, `metformin-renal-monitoring-question`, `nsaid-ace-inhibitor-question`, `renal-function-ace-question`, `statin-ck-myopathy-question`, `warfarin-antibiotic-question`) — none of these ever had a valid document citation survive to reach the semantic-support judge in the first place. This part of the finding is also unaffected by issue #100.

## Live re-verification (issue #100): the 5/12 baseline did not reflect current live behavior

A deep-dive diagnostic found that re-running `bp-stage2-question` LIVE (a genuine fresh draw through `evals/runner/pipeline.run_case`, production `LlamaServerClient`, no replay) landed on `partially_verified` with zero `document_citations` every time. Because the committed baseline above is deliberately measured against a **frozen, already-committed recording** rather than a fresh draw (precisely to insulate it from the run-to-run variance disclosed earlier in this document), that frozen recording had never been checked against what a fresh live draw of the same question actually produces today. This section closes that gap: all 5 of the baseline's non-xfail cases were re-run live, repeatedly, against current `main` (post PR #94/#95/#98/#101).

**Method.** Each case was driven through the real pipeline the same way `evals/runner/record.py` does for recording, but without committing a recording after every run — 8 to 10 independent fresh draws per case, same production `LlamaServerClient`/model/settings the committed baseline uses, against the dev stack's live `llama-server` container (Qwen3-8B-Q5_K_M).

**Result: none of the 5 reliably reproduce `verified` today.**

| Case | Live draws | `verified` | Typical outcome |
|---|---|---|---|
| `bp-stage2-question` | 10 | 0/10 (0%) | `partially_verified`, zero document citations — chart-data-only answering |
| `a1c-target-question` | 8 | 1/8 (12.5%) | `partially_verified` — guideline chunk IS cited every run, but a co-occurring chart-data claim fails verification often enough to keep the verdict below `verified` |
| `dual-antiplatelet-question` | 8 | 0/8 (0%) | `blocked` (fail-closed), zero document citations — chart-data-only answering |
| `hypertension-lifestyle-followup-question` | 8 | 0/8 (0%) | `blocked` (fail-closed), zero document citations — chart-data-only answering |
| `lithium-nsaid-question` | 8 | 0/8 (0%) | `partially_verified` every run; the guideline chunk is cited in only 4/8 of those |

**Total across all 5 cases: 1 `verified` outcome in 42 independent fresh live draws.** This is not a one-off unlucky sample — it is overwhelming, repeated evidence that the committed 5/12 number was a property of one historical, no-longer-representative recording per case, not a number a clinician using the live system today should expect to see reproduced. Three of the five (`bp-stage2-question`, `dual-antiplatelet-question`, `hypertension-lifestyle-followup-question`) fail deterministically — same outcome on every single draw — which rules out ordinary sampling noise as the explanation; the model consistently does not surface/quote the retrieved guideline text for these questions under current main. The other two (`a1c-target-question`, `lithium-nsaid-question`) are less than fully deterministic but still fail the overwhelming majority of the time (87.5–100%).

**Action taken:** all 5 recordings (`evals/recordings/{bp-stage2-question,a1c-target-question,dual-antiplatelet-question,hypertension-lifestyle-followup-question,lithium-nsaid-question}.json`) have been re-captured against current `main` with a typical (non-passing) live draw, replacing the stale recordings, and each case is now honestly marked `xfail` in its YAML with a rationale describing exactly what was observed (see `evals/cases/citation_present/`). No recording was doctored to force either a pass or a fail — each re-capture used `evals/runner/record.py` against the real live model and, where a case showed some variance (`a1c-target-question`, `lithium-nsaid-question`), the captured recording matches the statistically typical (non-passing) outcome already established across the repeated live draws above, not a cherry-picked one.

**The honest committed number, updated: 0 of 12 `citation_present` cases currently pass.** All 12 cases are now documented `xfail`. `evals/category_baseline.json`'s P3G.2 gate treats an all-`xfail` category as a vacuous pass (nothing left to regress — see `CategoryStats.pass_rate`'s documented behavior), so CI stays green; the category's `_note` has been updated to state the honest 0/12 total plainly rather than let the gate's vacuous-pass numeric value imply otherwise.

This does not change the diagnosis already given above for *why* the ceiling is low on this hardware tier (model capacity, chart-data-only answering, extraction-decode limits) — if anything it confirms that diagnosis applies more broadly than the committed baseline previously showed. It changes only how much of that ceiling this project can currently claim to have measured a live model actually clearing.

### Claim-extraction citation routing bug fixed (issue #85) — a second, distinct mechanism found

Issue #100's `bp-stage2-question` deep-dive ("the model consistently does not surface/quote the retrieved guideline text") turned out to be imprecise for that one case: issue #85's follow-up live investigation found the model DOES attempt to cite the guideline chunk every time, but qwen3-8b puts the citation in `source_refs` (reconstructing the chunk's own `<doc_id>#<section-slug>` id across `tool_call_id`/`record_id`) instead of `document_citations` — a citation-routing defect in `app.extraction.ClaimExtractor`, not a "never tries" defect. `check_source_ref` correctly failed it `unknown_tool_call`, and `check_claim`'s AND-across-citations rule dragged the whole claim down with it (including two valid chart-data citations bundled in the same claim), which is why it looked identical to "zero document_citations, chart-data-only" from the outside. Fixed: the extractor now recognizes this specific, safely-identifiable shape and reclassifies it into a real `document_citations` entry before verification.

**This does not move `bp-stage2-question` off `xfail`.** With the routing fixed, the citation now reaches the semantic-support judge for the first time (it never did before, since the claim always failed provenance first) — and the judge correctly downgrades it `not_semantically_supported`: the model's own final-answer text calls 148/94 mmHg "elevated blood pressure," but the guideline's own thresholds put that reading in "Stage 2 hypertension." That is a planner answer-composition defect (the planner composes its free-text answer before evidence retrieval ever runs — see `app.chat`'s `planner.run()` vs. `evidence_retriever()` call ordering — so it never sees the guideline thresholds it's describing), not a claim/citation-assembly defect, and is out of issue #85's scope.

**Whether this same citation-routing mechanism explains the other 4 non-`bp-stage2` xfail'd cases in this baseline was not re-verified as part of issue #85** (`a1c-target-question`, `dual-antiplatelet-question`, `hypertension-lifestyle-followup-question`, `lithium-nsaid-question`) — issue #85's scope was `bp-stage2-question` only. Given the mechanism is a generic property of `app.extraction.ClaimExtractor` (not specific to the blood-pressure corpus text), it plausibly affects any of the other 4 cases where the model attempts a guideline citation at all; this is flagged here as a candidate explanation worth checking in a follow-up, not asserted as confirmed.

### Issue #106 follow-up: the routing bug does NOT reproduce elsewhere — a different mechanism found instead

The candidate flagged directly above was checked. All 4 of the other originally-flagged cases, plus 7 further `citation_present` cases already `xfail` for older reasons, were re-run live (`evals/runner/pipeline.run_case` / `evals/runner/record.py`, production `LlamaServerClient`, no replay; 3–11 fresh draws per case). **None of the 11 reproduce the `#85` misrouted-`source_refs` shape.** Inspecting the extractor's pre-coercion output directly (before `_coerce_misrouted_guideline_refs` runs) confirms this for every case that surfaces a citation at all: the model either emits a correctly-shaped `document_citations` entry on its own, or never attempts a guideline citation in any shape. `_coerce_misrouted_guideline_refs` was therefore left unchanged — there was no shape variant observed live to generalize it against, and inventing one to "future-proof" the coercion without a live-observed trigger would risk exactly the kind of speculative broadening the function's existing safety comments warn against.

Two cases turned out to be materially affected by #85's fix anyway, just not via the routing-bug mechanism: `dual-antiplatelet-question` and `hypertension-lifestyle-followup-question` still fail 6/6 draws exactly as #100 described (chart-data-only answering, zero guideline attempt) — unchanged. But `a1c-target-question` and `lithium-nsaid-question` now reliably (6/6 and 11/11 draws respectively) get their guideline citation into `document_citations` and past provenance re-validation — a real improvement over #100's 12.5%/50% figures. Both still fail to reach `verified`, for a **newly-identified, third mechanism**: the model restates the same guideline-backed fact as two separate claims in its answer (e.g. "...above the target range..." / "...is not at target."), both citing the identical chunk — and the semantic-support judge is inconsistent across the repeat, validating one occurrence while downgrading the other to `not_semantically_supported`. This is neither the `#85` routing bug nor the `#105` planner-composition/category-mismatch defect (the category language is correct in both cases); it looks like a claim-splitting / semantic-support-judge-consistency gap and is documented as a candidate for a new follow-up issue rather than fixed here. See `evals/cases/citation_present/a1c-target-question.yaml` and `lithium-nsaid-question.yaml`'s `xfail` rationales for the full per-case detail; both recordings have been re-captured against current main with a statistically typical draw.

Separately, `statin-ck-myopathy-question`'s recording — stuck on a stale qwen3:4b-era capture since issue #58 because `LlamaServerError` didn't extend `LLMEngineError` at the time and crashed the recorder — was re-recorded successfully (4/4 draws, no crash): a later, unrelated fix (`2483e3b`) already gave `LlamaServerError` the right base class. The case shows the same chart-data-only pattern as before, now confirmed on the current production model rather than the retired 4B one.

**The `citation_present` category total is unchanged at 0/12** — this follow-up sharpens *why* 2 of the 12 cases fail (citation-routing is fixed for them; a different judge-consistency gap is the actual blocker now) without changing the honest bottom line stated above.

### Issue #105 follow-up: planner/retrieval-ordering fix — the category-mismatch mechanism is fixed, but the case still does not verify live

Issue #85's follow-up above diagnosed `bp-stage2-question`'s remaining failure as an architecture defect: `app.chat`'s guideline-corpus retrieval ran strictly *after* `planner.run()` composed the final answer text, so the planner described 148/94 mmHg as "elevated blood pressure" from its own training priors while the guideline it went on to cite categorizes that same reading as "Stage 2 hypertension" — a genuine, verbatim citation attached to prose using the wrong category name for what it cited.

**Fix implemented:** retrieval now runs *before* the planner call (`app.chat._stream_chat`), and the retrieved guideline text is threaded into the planner's own answer-composition call (`Planner.run`/`run_streaming`'s new `guideline_excerpts` parameter, consumed by `_finalize_answer_streaming`) with an explicit instruction to use the guideline's own category name rather than substitute general terminology.

**Live re-verification (11 fresh draws, production `LlamaServerClient`, `evals.runner.pipeline.run_case`, no replay):** the category-mismatch mechanism is genuinely fixed — **11/11 draws** now compose the answer as "...falls into the category of Stage 2 hypertension," matching the guideline's own category name exactly, and the claim extractor correctly attaches a real, verbatim `document_citations` entry quoting `"Stage 2 hypertension: systolic 140 mmHg or higher OR diastolic 90 mmHg or higher."` — the citation passes provenance re-validation every time.

**The case still does not reach `verified`, in all 11 draws, for a *different* reason than before.** The semantic-support judge now downgrades the citation with a new objection: *"The QUOTE defines Stage 2 hypertension but does not provide the patient's blood pressure reading, so it does not support the specific claim about the patient's condition."* The judge wants the cited guideline excerpt to *also* restate the patient's own 148/94 mmHg reading, which a category-threshold reference document by design never will — that reading only ever lives in the chart-data citation on the claim's own `source_refs`, a separate citation on the SAME claim the judge is not shown as connected context. This is a **new, narrower, and out-of-scope defect**: the semantic-support judge's per-citation evaluation doesn't appear to account for a claim being jointly supported by a chart-data citation (the value) plus a guideline citation (the category the value falls in) — it evaluates the guideline citation in isolation and correctly, on that narrow question, finds it insufficient alone. Fixing that is a claim/citation-assembly or judge-prompt change, not a planner answer-composition one, and is out of this issue's (#105) stated scope (bp-stage2's category-name mismatch specifically) — flagged here as a candidate for a new follow-up issue rather than fixed as part of this PR.

**The `citation_present` category total remains 0/12.** `bp-stage2-question` stays `xfail` — its `xfail` rationale has been updated to describe this new mechanism rather than the now-fixed category-mismatch one, so a future reader isn't misled into thinking the case is blocked on a problem that no longer exists.

### Issue #108 follow-up: duplicate-claim judge inconsistency fixed — the category total moves to 2/12

Issue #106's follow-up above identified a third mechanism blocking `a1c-target-question` and `lithium-nsaid-question`: both cases reliably got their guideline citation into `document_citations` and past provenance, but the model's answer restates the same guideline-backed fact as two separate claims (e.g. "...above the target range..." / "...is not at target." for `a1c-target-question`; the interaction caution and a separate monitoring-advice sentence for `lithium-nsaid-question`), both citing the **exact same** `DocumentCitation` (identical `source_type`/`source_id`/`field_or_chunk_id`/`quote_or_value`, byte-for-byte — confirmed by inspecting the extractor's pre-judge claim list directly). The semantic-support judge (`app.semantic_support.apply_semantic_support`) judged each claim's citation with its own independent LLM call and scored the two inconsistently — typically validating one restatement while downgrading the other (usually the terser one) to `not_semantically_supported` — purely from call-to-call variance on the paraphrased wording, not from any real difference in the underlying evidence. That stripped one of the two claims, holding the verdict at `partially_verified` in `a1c-target-question`'s case 5/6 draws and deterministically in `lithium-nsaid-question`'s case 11/11 draws (see issue #106's numbers above).

**Root cause, confirmed by tracing `app.semantic_support`, not guessed:** `apply_semantic_support` iterated per-``ClaimCheckResult`` and issued one independent judge call per claim's `DocumentCitation`, with no shared state across claims. Two claims citing the identical evidence therefore always received two independent LLM calls, with no mechanism to keep them consistent — the model's own judge nondeterminism on near-duplicate wording was the entire source of the disagreement, since the evidence backing both calls was provably byte-identical.

**Fix (`app/semantic_support.py`, PR closing issue #108):** `apply_semantic_support` now runs in two passes. The first groups every currently-`VALID` `DocumentCitation` result across ALL already-passing claims by its exact evidence identity (`source_type`, `source_id`, `field_or_chunk_id`, `quote_or_value` — a byte-for-byte match, never a fuzzy/paraphrase merge) and judges each distinct identity **exactly once**, against the combined text of every claim that cites it. The second pass applies that one shared verdict back to every citation result sharing the identity. Two claims citing genuinely identical evidence can therefore no longer land on different verdicts — and, as a side effect, fewer judge calls are made whenever this duplication occurs. Citations with distinct identities (even quoting the same source document) are still judged fully independently, exactly as before this fix; the change is scoped to this one narrow shape and does not touch `app.verification`, `app.extraction`'s claim-building, or `app.verdict`.

**Live re-verification (production `LlamaServerClient`, `evals.runner.pipeline.run_case`, no replay, inside the dev stack's `development-easy-agent-1` container against `development-easy-llama-server-1`):**

| Case | Live draws (post-fix) | `verified` |
|---|---|---|
| `a1c-target-question` | 8 | 7/8 (87.5%) |
| `lithium-nsaid-question` | 8 | 8/8 (100%) |

`a1c-target-question`'s one non-`verified` draw failed for a **different, pre-existing, unrelated mechanism**: that draw's answer never attempted the guideline citation at all (zero `document_citations`, chart-data-only) — the same planner/extraction variance already disclosed throughout this document, not a judge-consistency defect. It is not a regression introduced by this fix, and is not claimed as fixed here.

**Relationship to issue #111, checked explicitly as instructed by that issue's "possibly related" note:** #111 (`bp-stage2-question`'s semantic-support judge evaluating a guideline citation in isolation from a *different*, sibling chart-data citation on the *same* claim) is a **different root cause from #108's**, confirmed by tracing both through the same module. #108's shape is two *separate claims* citing the *identical* `DocumentCitation` object — fixable by deduplicating identical evidence before judging, with no change to what the judge sees for any single citation. #111's shape is *one claim* with *two different* citations (a category-threshold guideline citation and a chart-data value citation), where the judge only ever sees one citation at a time and has no way to know the claim's other citation already established the value — that requires the judge to see a claim's full citation set together, a materially different (and larger) change than #108's exact-identity dedup. Fixing #108 did not fix, and was never expected to fix, #111 — `bp-stage2-question` remains blocked by #111's mechanism, unchanged by this PR, and is explicitly out of this issue's scope.

**Both cases' eval YAMLs updated:** `evals/cases/citation_present/a1c-target-question.yaml` and `lithium-nsaid-question.yaml` have had their `xfail` rationale removed (replaced with a plain comment describing the fix and the live-verification numbers above) and fresh, honest `verified` recordings committed (`evals/recordings/{a1c-target-question,lithium-nsaid-question}.json`) — the first live attempt at re-recording each case landed on `verified` for both, consistent with the now-typical (87.5%/100%) live outcome, not a cherry-picked draw.

**The `citation_present` category total moves from 0/12 to 2/12.** `a1c-target-question` and `lithium-nsaid-question` are the first two cases in this document's history to reliably reach `verified` against a genuinely fresh, non-replayed live draw (not merely a stable historical recording, per issue #100's methodology critique). The other 10 cases are unaffected by this fix and remain `xfail` for the reasons already documented above (`bp-stage2-question`: #111's isolated-citation-judging mechanism; `dual-antiplatelet-question`/`hypertension-lifestyle-followup-question`: chart-data-only answering, zero guideline attempt; the remaining 6: chart-data-only/extraction-decode patterns tracked since the exploratory sweep and #58).

### Issue #109 findings: root-cause bucketing the remaining "chart-data-only" cases

Issue #109 asked a narrow question about a label, not a mechanism: #100/#106 called 10 of the 12 `citation_present` cases "chart-data-only answering," but that phrase describes a *symptom* (the answer has no guideline citation) that at least four structurally different causes could produce — a corpus-coverage gap, the 0.75 relevance floor (`_EVIDENCE_MIN_RELEVANCE_SCORE`, issue #99) dropping a real match, the planner genuinely ignoring evidence in front of it, or an eval case that was never really citable to begin with. Fixing the wrong one wastes effort and risks the same mistake issue #93's original filing made.

**Method.** For each of the 9 in-scope cases (`dual-antiplatelet-question`, `hypertension-lifestyle-followup-question`, `lipid-panel-ldl-question`, `metformin-renal-monitoring-question`, `nsaid-ace-inhibitor-question`, `renal-function-ace-question`, `statin-ck-myopathy-question`, `statin-liver-monitoring-question`'s chart-data-only draws, `warfarin-antibiotic-question`):

1. The question was run through the REAL production retrieval + reranking pipeline directly (`app.chat._build_evidence_workers`'s exact wiring — real corpus, real `nomic-embed-text` embedder via `llama-server-embed`, real qwen3-8b reranker via `llama-server`), bypassing the planner entirely, to get the actual live relevance score for whatever chunk retrieval surfaces — NOT the fixture's hardcoded `rerank_score: 0.9`, which the eval harness's `pipeline.run_case` injects directly into `guideline_excerpts` regardless of the floor (the harness never calls `_filter_by_relevance_score` at all — that filter is a `/chat`-boundary-only concern, so a fixture-only re-run can never observe bucket-2 filtering; the live retrieval probe above is the only way to see it).
2. Where the live score cleared 0.75, the case was re-run 3+ times live (`evals.runner.pipeline.run_case`, production `LlamaServerClient`, no replay, inside `development-easy-agent-1` against `development-easy-llama-server-1`) with claim-level instrumentation (`app.extraction.ClaimExtractor.extract_claims` + `app.verification.check_claims` called directly, mirroring `run_verification`'s own steps) to see whether the guideline excerpt's `DocumentCitation` actually attaches and passes provenance, not just whether the overall verdict reaches `verified`.

**Results:**

| Case | Live rerank score (real pipeline) | Clears 0.75 floor? | Live guideline citation attaches? | Bucket |
|---|---|---|---|---|
| `lipid-panel-ldl-question` | 0.62 (`lipid-panel-reference#general-reference-categories`, the only relevant chunk retrieved) | **No** | Never reaches the planner in production | **Bucket 2 — relevance-floor filtering** |
| `dual-antiplatelet-question` | 0.85 (`anticoagulant-interactions#dual-antiplatelet-therapy`) | Yes | Yes, deterministically (7/7 draws across two runs) — a real, verbatim `DocumentCitation` that passes provenance | **New mechanism (below), not 1-4** |
| `hypertension-lifestyle-followup-question` | 0.85 / 0.75 (two chunks) | Yes | Yes, deterministically (3/3 draws) | **New mechanism (below), not 1-4** |
| `metformin-renal-monitoring-question` | 0.95 (`renal-function-monitoring#metformin`) | Yes | Yes, cleanly — `verified` 3/3 fresh draws | **Eval-recording staleness** |
| `nsaid-ace-inhibitor-question` | 0.85 (`nsaid-interactions#ace-inhibitors-and-arbs`) | Yes | Yes — `partially_verified` 3/3, reproducing its already-documented near-miss exactly | Confirms existing docs; not chart-data-only |
| `renal-function-ace-question` | 0.95 (`renal-function-monitoring#ace-inhibitors-and-arbs`) | Yes | Yes — `partially_verified` 3/3, reproducing its already-documented near-miss exactly | Confirms existing docs; not chart-data-only |
| `statin-ck-myopathy-question` | 0.95 (`statin-monitoring#muscle-symptoms-and-ck`) | Yes | **Inconsistent** — 1/3 draws attempted no guideline citation at all; 2/3 attached a valid one | **Bucket 3 — genuine inconsistent citation attempt** |
| `statin-liver-monitoring-question` (chart-data-only draws) | 0.95 (`statin-monitoring#baseline-liver-function`) | Yes | Yes — 2/3 draws `verified`, 1/3 `partially_verified` on an unrelated empty-citation claim; no chart-data-only draw observed in this sample | Inconclusive on recurrence; likely improved by #105 |
| `warfarin-antibiotic-question` | 0.85 (`anticoagulant-interactions#warfarin-specific-cautions`) | Yes | Yes, twice per draw — `verified` 3/3 fresh draws | **Eval-recording staleness** |

**Bucket 2 confirmed once: `lipid-panel-ldl-question`.** This is the one case in scope where the corpus genuinely has the right content (`lipid-panel-reference#general-reference-categories`, the exact chunk the eval fixture already uses) but the *production* qwen3-8b reranker scores it 0.62 for this query — clearing neither the 0.75 floor calibrated in issue #99 nor a lower bar with real margin. Because the eval harness's `retrieved_chunks` fixture is injected directly into `guideline_excerpts` without ever calling `_filter_by_relevance_score`, this filtering was invisible to every prior recording/xfail investigation of this case — the fixture-driven re-runs could only ever show "the model doesn't cite it even when handed it," never "production would never hand it to the model in the first place." Both are true here, and the floor is the one that actually governs live behavior.

**A previously-undocumented mechanism found for `dual-antiplatelet-question` and `hypertension-lifestyle-followup-question`: neither is actually "chart-data-only."** Both cases' existing `xfail` rationales say the model "never quotes the retrieved chunk verbatim" — that turned out to be stale. Direct claim-level instrumentation shows the model attaches a real, verbatim `DocumentCitation` deterministically (dual-antiplatelet: 7/7 draws across two separate runs; hypertension-lifestyle-followup: 3/3), and it passes provenance re-validation (`DocumentCitationCheckResult: valid`) every time. The reason the overall verdict still lands on `blocked` is unrelated to the guideline citation at all: **every ordinary chart-data claim's `SourceRef` fails re-validation with `unknown_record`**, including trivial claims copied directly from a real tool result (e.g. "The patient is currently taking both aspirin and clopidogrel," sourced from `get_medications`). Tracing one instance directly (`app.extraction.ClaimExtractor.extract_claims`'s raw output before `check_claims` runs) shows the extractor citing `tool_call_id='call_0', field='medications', asserted_value='aspirin'` for that claim in a turn where `call_0`'s actual raw result was `get_problems`' *empty* problem list — the model's own claim-extraction call mislabeled (or, in at least one draw, effectively fabricated) which tool call a chart fact came from. Because `check_claim`'s AND-across-citations rule fails a claim if *any* one of its citations fails, and because in these two cases the guideline-cited claims are interleaved with claims carrying this SourceRef defect, the answer as a whole can still fail to verify even though the guideline half of the work is functioning correctly. **This is a citation/extraction-layer defect, not a retrieval, relevance-floor, or planner-composition one, and it is out of #109's scope to fix** — flagged here with concrete evidence and filed as a follow-up (see below) rather than fixed in this spike.

**Bucket 3 confirmed once: `statin-ck-myopathy-question`.** Unlike the two cases above, this one's own citation behavior is the inconsistent part: the same question, same retrieved chunk (0.95, comfortably above the floor), same `guideline_excerpts` context reaching the planner every time, produced a draw with zero guideline citation attempt at all (all three non-medication claims failed `unknown_tool_call`, meaning the extractor never even produced a `DocumentCitation` for them) alongside two draws where a valid `DocumentCitation` was attached. This is the one case in scope that is a genuine, actionable "planner/extractor sometimes ignores evidence in front of it" finding — the only shape in this whole investigation that actually matches bucket 3's definition.

**Two cases (`nsaid-ace-inhibitor-question`, `renal-function-ace-question`) simply confirm what was already documented and are not, and were never observed live to be, chart-data-only** — their existing `xfail` rationale (a valid guideline citation, held below `verified` by one or two unrelated chart-data claims losing their `SourceRef`) reproduced exactly, 3/3 draws each. They were carried into #109's scope by #100/#106's blanket "chart-data-only" framing, but the live evidence says that framing never actually applied to them.

**Eval-recording staleness: `metformin-renal-monitoring-question` and `warfarin-antibiotic-question` no longer reproduce chart-data-only behavior at all.** Both verified cleanly 3/3 in fresh live draws, with valid guideline citations and clean chart-data `SourceRef`s throughout — the same pattern issue #108 already found and fixed for `a1c-target-question`/`lithium-nsaid-question`. Their committed recordings and `xfail` rationale predate whatever combination of #85/#105/#108 fixed this (not reproduced here — a live re-verification only establishes *that* it's fixed, not precisely *which* prior fix gets credit), and are stale in the same way those two cases' recordings were before #108. `statin-liver-monitoring-question`'s chart-data-only draws are inconclusive rather than confirmed-stale: 2/3 fresh draws verified and 1/3 failed for an unrelated reason, but zero of the 3 reproduced an actual chart-data-only draw, so the category this case was carved into scope for was not observed at all in this sample (a larger draw count would be needed to rule it out entirely, which is out of this spike's budget).

**The `citation_present` category total is unchanged at 2/12 by this spike** — no production code was changed here (measurement/diagnostic spike per issue #109's own scope), and updating `metformin-renal-monitoring-question`/`warfarin-antibiotic-question`'s recordings to reflect what they actually do today is left to the follow-up issue below (re-recording is a "re-verify and commit," not a code fix, but is sequenced as its own tracked follow-up rather than folded into this docs-only spike to keep this PR's diff to documentation only, matching #109's "no production code fix in this spike" instruction).

**Follow-up issues filed** (see "Key documentation" section of this repo's issue tracker for full text):
- Corpus/threshold-tuning candidate, `lipid-panel-ldl-question`'s bucket-2 finding (the 0.75 floor, or the LDL-reference chunk's phrasing/chunking, may need adjustment — evidence: 0.62 measured score, clean gap either way).
- Narrowly-scoped prompting-consistency issue for `statin-ck-myopathy-question`'s bucket-3 finding only (NOT a generic "improve prompting across all cases" issue).
- New citation-extraction defect: chart-data `SourceRef` misattribution, blocking `dual-antiplatelet-question` and `hypertension-lifestyle-followup-question` despite both having working guideline citations.
- Eval-recording staleness: re-verify and re-record `metformin-renal-monitoring-question` and `warfarin-antibiotic-question` (both now `verified` live), and take a larger live sample of `statin-liver-monitoring-question` to determine whether its chart-data-only draws still occur at all.

### Issue #116 follow-up: eval-recording staleness resolved for warfarin ONLY — metformin's original xfail rationale is stale but the case doesn't qualify — the category total moves to 3/12

Issue #109's spike found `metformin-renal-monitoring-question` and `warfarin-antibiotic-question` reaching `verified` 3/3 in fresh live draws each, but left the actual re-recording (and a larger, more confident sample) to a tracked follow-up rather than folding it into a docs-only spike. Issue #116 is that follow-up. **A larger sample changed the metformin conclusion** — the honesty discipline this issue was explicitly asked to apply ("if a case turns out NOT to reliably verify... say so plainly... do not force a pass") turned out to matter for real, not just as a caveat.

**Method.** Both cases were re-run 10 additional times each through the real pipeline (`evals.runner.pipeline.run_case`, production `LlamaServerClient`, no replay, inside `development-easy-agent-1` against `development-easy-llama-server-1`), mirroring the rigor issue #100/#106/#108 used for their own re-verification passes. Critically, the assertion actually checked here is the SAME one `evals/runner/assertions.py::_check_guideline_citation_present` uses — a surviving claim's `document_citations` containing a `source_type == "guideline_chunk"` entry — not just the overall `verdict` enum, which turned out to be an insufficient signal on its own (see below).

**`warfarin-antibiotic-question`: genuinely un-xfailed.** `verified` 10/10 draws, every draw identical, with two real `DocumentCitation`s per draw quoting `anticoagulant-interactions#warfarin-specific-cautions` verbatim, both passing provenance AND a live semantic-support judge call (`verdict: supported`) every time, zero stripped claims. Its `xfail` marker has been removed (`evals/cases/citation_present/warfarin-antibiotic-question.yaml`), replaced with a plain comment describing the re-verification — mirroring exactly how issue #108 handled `a1c-target-question`/`lithium-nsaid-question`. A fresh recording was captured against current `main` (`record_case`, `RECORD_ENGINE=llama_server`) and committed; the first re-record attempt landed on `verified`, consistent with the fully-deterministic live outcome, not a cherry-picked draw.

**`metformin-renal-monitoring-question`: the ORIGINAL chart-data-only rationale is stale, but the case does NOT qualify to un-xfail — a different, still-failing mechanism was found instead.** All 10 draws reach an overall `verified` verdict (matching issue #109's 3/3), and the model's answer prose reliably restates the guideline content. But inspecting `result.rendered.segments` directly (the same data `guideline_citation_present` checks) shows **zero of the 10 draws ever attach a real `DocumentCitation`** for the guideline quote. Instead, the claim extractor emits several (5-10) duplicate copies of the SAME, completely unrelated chart-data `SourceRef` (the medication-name citation, `tool_call_id=call_0, record_id=0, field=name`) on both guideline-derived claims. This is not `#85`'s misrouted-chunk-id shape — `_coerce_misrouted_guideline_refs` specifically reconstructs a `<doc_id>#<section>` pair from a ref's `tool_call_id`/`record_id`, and there is no such reconstructable pair here, so it correctly no-ops. Because the duplicated medication refs are trivially valid (real chart data) and `check_claim` only ANDs citation validity with no content-relevance check on ordinary `SourceRef`s, the claim survives re-validation with zero real support for its actual content — `verdict` reaches `verified` while `guideline_citation_present` correctly fails, 10/10. A direct pytest run of `evals/test_cases.py::test_case_replay[metformin-renal-monitoring-question]` against the fresh recording confirms this exactly: it fails on `guideline_citation_present`, not `verdict`.

**This corrects issue #109's own finding for this case.** #109's table entry ("Live guideline citation attaches? Yes, cleanly — verified 3/3 fresh draws") was evaluated too coarsely — trusting the overall verdict and a same-text-as-source-quote match in the model's prose, rather than checking `document_citations` on the surviving claims directly. This is the exact trap `_check_guideline_citation_present` was written to close: an answer that *sounds* like it cites the guideline is not the same as one whose extracted claims actually carry a verified `DocumentCitation`. Per this issue's own instruction not to force a pass, `metformin-renal-monitoring-question`'s `xfail` rationale has been rewritten (not removed) to describe this newly-observed extraction-misattribution mechanism precisely, and the case remains `xfail`. The freshly-captured, typical (still-failing) recording is committed in its place, replacing the now-stale chart-data-only one. A follow-up issue has been filed for the underlying extraction-misattribution mechanism itself (chart-data `SourceRef`s can rubber-stamp a claim with no relevance check, the same gap-shape as the `dual-antiplatelet-question`/`hypertension-lifestyle-followup-question` `SourceRef`-misattribution defect issue #109 already filed a follow-up for, though the exact relationship between the two is not established here and is left to that follow-up).

**Tooling note (not fixed here, out of #116's scope):** invoking `evals/runner/record.py` as a script (`python evals/runner/record.py <case>`) inside this project's `development-easy-agent-1` container currently imports a stale, pre-built `app` package from `site-packages` instead of the live source baked into the same image — `record.py`'s own `_AGENT_ROOT` path computation (`_EVALS_ROOT.parent / "services" / "copilot-agent"`) assumes a full-monorepo layout that doesn't match this container's flattened `/app` root, so its `sys.path` fix-up silently no-ops and `python`'s own script-directory-on-`sys.path[0]` behavior lets the stale installed copy shadow the live one. This surfaced as `TypeError: Planner.run() takes 2 positional arguments but 3 were given` (an old, pre-#105 `Planner.run` signature) even against a fully up-to-date checkout. Also discovered along the way: this container has **no bind mounts at all** (`docker inspect`'s `Mounts: []`) — it is a baked image copy, not a live-editable checkout, so any in-container file write (recordings included) must be copied out explicitly (`docker cp`) rather than assumed to land on the host automatically the way the dev-stack's other services do. Both workarounds were applied here (direct `record_case()` invocation with `/app`/`/app/evals` pre-inserted onto `sys.path`, then `docker cp` to pull the resulting recordings onto the host); real fixes belong in `evals/runner/record.py` and the container build respectively, both out of scope for this measurement/re-recording issue. Filed as follow-ups (see "Key documentation" section of this repo's issue tracker for full text).

**`statin-liver-monitoring-question`: the chart-data-only pattern does not recur at a larger sample — the case remains honestly `xfail`.** Issue #109's spike found this case's chart-data-only draws inconclusive (2/3 verified, 1/3 an unrelated stripped claim, zero draws reproducing an actual chart-data-only outcome). A larger sample of 10 additional live draws was taken, targeting specifically whether a chart-data-only draw (zero guideline citation attempt) occurs at all: **it did not, 0/10.** Every one of the 10 draws attempted the guideline citation, and the `statin-monitoring#baseline-liver-function` quote attached and passed provenance re-validation in every draw (`DocumentCitationCheckResult: valid`, correct direction — "routine monitoring not needed" — matching the source, not #81's previously-documented opposite-direction mismatch either). Combined with issue #109's 3 draws, that's **0/13 chart-data-only draws observed** — enough to say plainly that #109's inconclusive finding was a small-sample artifact, not a real recurring pattern, and this narrow question is now resolved.

That said, **the case still did not reach `verified` in any of the 10 draws** (10/10 `partially_verified`) — for reasons distinct from both the chart-data-only shape and the existing `#81`/`#105`/`#111`-documented direction-mismatch mechanism (which this sample also did not reproduce): most draws stripped one ordinary chart-data claim (e.g. "no recent vitals or lab results" losing its `SourceRef`), and one draw hit a `quote_not_found` on a paraphrased (non-verbatim) guideline restatement. This looks like the same shape as the chart-data `SourceRef`-misattribution defect issue #109 already found and filed a follow-up for (`dual-antiplatelet-question`/`hypertension-lifestyle-followup-question`), not a new mechanism — but that connection is not verified here and is out of this issue's scope to chase further. Per this issue's own instruction not to force a pass: `statin-liver-monitoring-question`'s existing `xfail` rationale (the `#81` category-mismatch finding) is left unmodified, since that is not the mechanism this sample's scope covers — the case remains `xfail`, and is **not** the "possible fifth" case issue #116 flagged as pending.

**The `citation_present` category total moves from 2/12 to 3/12.** `warfarin-antibiotic-question` joins `a1c-target-question` and `lithium-nsaid-question` as the third case in this document's history to reliably reach `verified` against a genuinely fresh, non-replayed live draw AND a genuine `guideline_citation_present`. `metformin-renal-monitoring-question` and `statin-liver-monitoring-question` were both evaluated for a possible fourth/fifth and neither qualifies — both remain `xfail`, each for a distinct, freshly-documented reason that is not the chart-data-only shape either was carved into #109's scope to check.

### Issue #115 findings: root-causing the chart-data `SourceRef` misattribution for `dual-antiplatelet-question`/`hypertension-lifestyle-followup-question` — genuine fabrication, not a recoverable routing shape; category total unchanged at 3/12

Issue #109 flagged, but explicitly left unfixed, a distinct defect behind these two cases: a real, verbatim guideline `DocumentCitation` attaches deterministically for both, yet the overall verdict still lands on `blocked` because every ordinary chart-data claim's `SourceRef` fails `unknown_record`. Issue #115 traces this precisely, per its own instruction to distinguish "consistently-malformed but recoverable" (issue #85's shape) from "genuinely fabricated" (unsafe to coerce) before touching any code.

**Method.** Both cases were re-run live and directly instrumented (`ClaimExtractor.extract_claims` + `app.verification.check_claims`/`check_claim`, mirroring `run_verification`'s own steps, against production `LlamaServerClient` inside `development-easy-agent-1`) — 6+ draws for `dual-antiplatelet-question`, 3+ for `hypertension-lifestyle-followup-question`. **Every draw was byte-identical** (the pipeline's default `temperature=0`, per `app.llama_server_client`), so "N draws" here means N independent confirmations of one fully deterministic outcome, not a statistical sample over run-to-run variance.

**The precise mechanism, traced to source.** Both questions state the relevant chart fact directly in the question text — `dual-antiplatelet-question`: "She's on both aspirin and clopidogrel"; `hypertension-lifestyle-followup-question`: "His blood pressure was elevated at his last visit and we're trying lifestyle changes." In both cases the **planner never dispatches the tool that would actually confirm the fact** — `get_medications` is never called for the first (trace: `[get_problems, get_allergies]`, both empty); `get_vitals` is never called for the second (trace: `[get_appointments, get_encounters]`, both empty). The planner answers directly from the question's own stated facts. When the claim extractor is then asked to decompose the answer and cite a source for "the patient is currently taking both aspirin and clopidogrel" (or "blood pressure was elevated," "lifestyle changes are being pursued," etc.), there is *no real tool-result catalog entry for it at all* — the catalog it is shown is genuinely empty (both real tool calls returned zero records). Rather than leaving the claim uncited (the schema explicitly permits this — issue #93 Option C — a citation-less claim simply fails cleanly downstream) or omitting the claim, the extractor **fabricates** a syntactically well-formed `SourceRef`: a real-looking `tool_call_id` (`call_0`, always a genuine call this turn — never `#85`'s misrouted-doc-id shape) paired with a plausible-sounding but entirely nonexistent `field` (`medications`, `allergies`, `conditions`, `blood_pressure`, `treatment_plan`, `next_appointment`, `scheduled_appointments`, `recent_encounters` — none of these ever appear in ANY real tool-result catalog entry for the conversation). `check_source_ref` correctly resolves `call_0` (a real call) but finds zero records at `record_id` (the call returned `{"items": []}`) → `UNKNOWN_RECORD`, exactly the status the original issue named. This is reproduced identically across every draw for both cases.

**The guideline-citation half of #109's finding holds, with one added wrinkle.** Both cases DO reliably attach a real, verbatim `guideline_chunk` `DocumentCitation` that passes provenance re-validation — `dual-antiplatelet-question` on 3 of 5 claims (quoting `anticoagulant-interactions#dual-antiplatelet-therapy`), `hypertension-lifestyle-followup-question` on 1 of 6 (quoting `hypertension-lifestyle#follow-up-cadence`). But on `dual-antiplatelet-question` specifically, each of those 3 guideline-cited claims ALSO carries a second, redundant, fabricated `source_ref` (`tool_call_id='call_0', record_id='1', field='clinical_guidelines', asserted_value=<the chunk's section title>`) — the extractor restating the guideline connection a second time through the wrong citation mechanism. Because `check_claim`'s AND-across-citations rule requires every citation on a claim to pass, this one bogus extra ref drags even the genuinely-cited claim down.

**Why no coercion fix was implemented.** Issue #85's `_coerce_misrouted_guideline_refs` recovers a *specific, reconstructable* shape: a ref whose `tool_call_id`/`record_id` decode to exactly `<doc_id>#<section>` for a chunk THIS turn actually retrieved. None of the fabricated refs found here have that shape — `field='medications'`/`'blood_pressure'`/etc. do not encode a retrievable chunk id, and even the one plausible-looking candidate (the redundant `field='clinical_guidelines'` ref on `dual-antiplatelet-question`, whose `asserted_value` happens to match the paired `DocumentCitation`'s `page_or_section`) would not move either case to `verified` even if dropped: `dual-antiplatelet-question`'s medications/allergies/conditions claims and `hypertension-lifestyle-followup-question`'s blood-pressure/appointments/encounters claims are separately, independently fabricated with no valid citation to fall back on, and `compute_verdict` requires ALL claims to pass for `Verdict.VERIFIED` (`_CitationState.ALL_VERIFIED`) — a genuinely fabricated majority of claims blocks the verdict regardless of any minority recovery. Building a coercion here would therefore be pure risk (teaching the checker to trust an LLM-asserted field name it cannot independently confirm against any real source, this project's core no-fabrication invariant) for zero measurable benefit to either target case. Per issue #115's own explicit instruction, this is flagged for the maintainer rather than shipped: `check_source_ref`'s `UNKNOWN_RECORD` rejection here is correct, fail-closed behavior, not a bug to route around.

**What WOULD actually fix these two cases (out of scope here, flagged for the maintainer).** The root enabling condition is upstream of `ClaimExtractor`: the **planner** treats a fact already stated in the user's question as established without ever confirming it via the corresponding tool call, so no real citable record exists for the extractor to point at. A genuine fix is either (a) prompting/forcing the planner to dispatch the relevant confirming tool call even when the question already states the value, or (b) prompting the extractor to leave a claim uncited when its fact is not in the real catalog rather than inventing a plausible-looking ref. Both are real, but larger and riskier, prompt-engineering changes to an already brittle small (8B) model — this project's own history (`_GUIDELINE_INSTRUCTIONS`' P3G.1b comment) documents in-place prompt rewordings measurably degrading unrelated, previously-working citations. Filed as issue #123 for the maintainer to prioritize and scope carefully, rather than folded into this fix.

**Regression guard added, no production code changed.** `services/copilot-agent/tests/test_verification_documents.py::test_fabricated_source_ref_alongside_valid_document_citation_still_fails_the_claim` hermetically reproduces the exact live shape (a real-tool-call-but-zero-records `source_ref` bundled alongside a genuinely valid `document_citations` entry on the same claim) and proves `check_claim`'s existing AND-aggregation already, correctly, fails the whole claim — locking in this fail-closed behavior against future erosion.

**The `citation_present` category total is unchanged at 3/12.** Both cases' `xfail` rationale has been rewritten (not removed) to describe this precisely-traced mechanism, superseding the stale #100-era "never quotes the guideline verbatim" reasoning (which is no longer accurate — both cases DO attach a real, verbatim guideline citation; that was never the blocker). Fresh, currently-reproducible recordings have been committed in their place.

### Issue #113 findings: the relevance floor is not the real blocker for `lipid-panel-ldl-question` — the 0.62 score was scorer variance, not a systematic floor miss; a distinct extraction-layer defect is the actual cause, filed separately; category total unchanged at 3/12

Issue #109's spike measured `lipid-panel-reference#general-reference-categories` (the corpus's only relevant chunk for this question) at **0.62** against the live qwen3-8b reranker for `lipid-panel-ldl-question`'s question — below `_EVIDENCE_MIN_RELEVANCE_SCORE` (0.75, issue #99), and filed as a corpus/threshold-tuning candidate. Issue #113 is that follow-up: confirm the finding directly, understand whether 0.75 is genuinely too strict for this topic, and fix it narrowly and safely if so.

**Method.** The exact same live wiring #109 used (`app.chat._build_evidence_workers` — real corpus, real `nomic-embed-text` embedder via `llama-server-embed`, real qwen3-8b reranker via `llama-server`) was queried for this question 10 additional times to check score *stability*, not just re-confirm the single number. Separately, `evals.runner.pipeline.run_case` (production `LlamaServerClient`, no replay) was re-run 10 times with claim-level instrumentation, mirroring #109/#115/#116's own method, to see whether the case now reliably reaches `verified`.

**Finding 1: the 0.62 score does not reproduce reliably — it was a low outlier, not the typical value.** Across 10 fresh live rerank draws of the identical (query, chunk) pair, the chunk scored **0.85 in 9/10 draws and 0.62 in 1/10** — the same non-determinism `app.llama_server_client`'s `temperature: 0` default does not fully eliminate for an LLM-as-judge call on CUDA (batch-composition-dependent floating point rounding is a known llama.cpp/CUDA characteristic, not a bug in this codebase). This means #109's single-draw 0.62 measurement was not a stable "this chunk sits below the floor for this topic" signal — in practice, this exact (query, chunk) pair clears `_EVIDENCE_MIN_RELEVANCE_SCORE` a large majority of the time in production.

**Finding 2: lowering the floor to reliably admit the 0.62 outlier would be unsafe regardless.** Issue #99's own calibration data (`app/data/reranker_scores_qwen3-8b.json`) already contains two deliberately-planted lexical distractors scoring **0.65** — the ibuprofen/lisinopril distractor (`renal-function-monitoring#ace-inhibitors-and-arbs`) and one other 0.65 entry — both *above* the 0.62 outlier this case's genuine chunk occasionally draws. Any floor low enough to catch 0.62 reliably (e.g. anywhere at or below 0.65) would simultaneously readmit those distractors, exactly the failure mode #99 was filed to close. Notably, `lipid-panel-reference#general-reference-categories` is ITSELF one of #99's planted distractors for a different golden query (`"What A1c target for most adults?"`, scoring a safe 0.35 there) — so this chunk's relevance is already known to be highly query-dependent, which is consistent with (rather than contradicting) the observed score instability for its own topic. **No threshold change was made.** Per this issue's own instruction, a global or topic-scoped floor lowering was rejected as unsafe rather than force-fit.

**Finding 3: reworking the chunk's phrasing/splitting (the issue's alternative hypothesis) was not pursued** — the score-variance finding above means the chunk already clears the floor most of the time as currently written; splitting it into a dedicated LDL-only chunk would also require re-deriving `scripts/reranker_golden_distractors.py`'s existing A1c-query distractor mapping (which specifically depends on today's `lipid-panel-reference#general-reference-categories` chunk id/text) and re-running the full `scripts/build_reranker_scores.py` calibration — real, but out-of-proportion work for a problem the floor mostly does not have. Left undone; flagged here rather than attempted speculatively.

**Finding 4: even when the chunk clears the floor and reaches the planner, the case still does not reach `verified` — for an unrelated, newly-discovered extraction-layer defect.** All 10 fresh live draws via `evals.runner.pipeline.run_case` (guideline chunk injected directly, bypassing the floor entirely — mirroring how #109/#115/#116 isolated this layer) were byte-identical (`temperature=0`) and reached `partially_verified`, never `verified`. The model reliably quotes the LDL reference-range guideline text verbatim in its answer, and the claim extractor reliably attempts to cite it — but as a misrouted `source_ref` (`tool_call_id="lipid-panel-reference"`, `record_id="lipid-panel-reference"`, `field="text"`) rather than a `document_citations` entry. This is a NEW variant of the issue #85-lineage `SourceRef`-misrouting defect: #85's existing `_coerce_misrouted_guideline_refs` coercion only reconstructs a chunk id from `tool_call_id="<doc_id>"` + `record_id="<section-slug>"`; here the model puts the **doc_id in both fields** (no section slug), so the reconstructed id (`"lipid-panel-reference#lipid-panel-reference"`) never matches the real chunk id (`"lipid-panel-reference#general-reference-categories"`), and the ref correctly fails `UNKNOWN_TOOL_CALL`. Unlike issue #115's finding for `dual-antiplatelet-question`/`hypertension-lifestyle-followup-question`, the guideline quote here is genuine and verbatim, not fabricated — this is a narrowly recoverable routing gap, not a fabrication. Filed separately as **issue #125** (out of #113's own relevance-floor scope) rather than fixed in this investigation.

**No production code changed.** `lipid-panel-ldl-question`'s `xfail` rationale has been rewritten (not removed) to describe this precisely-traced mechanism, superseding the stale #58-era "never surfaces or quotes the guideline passage" reasoning (no longer accurate — the model does surface and quote it; the citation is just misrouted). A fresh, currently-reproducible recording has been committed in its place. **The `citation_present` category total is unchanged at 3/12.**

### Issue #114 findings: `statin-ck-myopathy-question`'s chart-data-only pattern does not reproduce at a larger sample — no prompting change made; category total moves to 4/12

Issue #106 found `statin-ck-myopathy-question` failing 4/4 live draws with the model never attempting a guideline citation at all (chart-data-only answering). Issue #109 flagged it again as a candidate for the "planner/extractor sometimes ignores evidence directly in front of it" mechanism. Issue #114 asked for a larger live sample (8-10+ draws) to confirm the rate precisely and, if it turned out to be a genuine prompting gap, a narrowly-scoped fix (e.g. a few-shot addition similar in spirit to issue #98's vitals few-shot mitigation).

**Method.** Three separate live batches were run against current `main`, unmodified, via `evals.runner.pipeline.run_case` (production `LlamaServerClient`, no replay), inside `development-easy-agent-1` against `development-easy-llama-server-1`: 10 draws, then 10 more, then 5 more — 25 draws total, double the issue's own requested sample size.

**Finding: the chart-data-only pattern does not reproduce at scale.** 23 of 25 draws (92%) reached `verified` with a real, verbatim `statin-monitoring#muscle-symptoms-and-ck` `guideline_chunk` `DocumentCitation` on 3 of 4 claims, passing both provenance re-validation and the semantic-support judge every time. This is a dramatically different picture from #106's original 0/4 and from #109's own informal 1/3. The most plausible explanation is that other production-code fixes already merged earlier in this same citation-repair sequence — issue #105's fix threading retrieved guideline text into the planner's own answer-composition call (`guideline_excerpts`, `app.planner._finalize_answer_streaming`), and issue #108's semantic-support duplicate-claim judge fix — improved this specific case as a side effect, without anyone re-measuring it since #106's smaller, pre-#105 sample. This is the same "small-sample finding turned out stale" pattern issues #113 and #116 also found elsewhere in this document.

**The 2 non-passing draws were not root-caused — an honest gap, not a hidden one.** Both misses were in the first of the three batches, captured via a one-off diagnostic script whose output was piped through `tail -80` inside a backgrounded shell command; that truncation destroyed the individual per-draw detail for those 2 draws before they could be inspected (only the batch's aggregate 8/10 summary survived). Per this issue's own conservative instruction — do not force a fix without reproducing and understanding the mechanism it's meant to address — **no prompting or extraction-code change was made.** A static read of `app.extraction._GUIDELINE_INSTRUCTIONS` (the claim extractor's citation-attachment nudge, added by issue #85's prompt-engineering iteration) shows its explicit "cite this too" trigger is scoped to "a claim states a lab or vital value and a passage below gives its reference range or target" — a shape that does not literally describe this case (a monitoring-recommendation guideline, not a value-plus-threshold one), which was a plausible hypothesis for why an inconsistency *could* occur. But with the case already at 92% live reliability and no reproduced failing draw to verify that hypothesis against, inventing a nudge for a gap that mostly doesn't show up live would be exactly the kind of speculative, unverified prompt tinkering this batch's conservative posture is meant to prevent.

**`statin-ck-myopathy-question`'s `xfail` marker has been removed.** The first live re-record attempt (via a corrected version of `evals/runner/record.py`'s logic, run inside the agent container) landed on `verified`, consistent with the now-typical 92% outcome, not a cherry-picked draw. **The `citation_present` category total moves from 3/12 to 4/12** — `a1c-target-question`, `lithium-nsaid-question`, `warfarin-antibiotic-question`, and now `statin-ck-myopathy-question`.

### Issue #125 findings: the doc_id-doubled SourceRef-misrouting variant is fixed — but a separate, newly-discovered semantic-support judge defect (filed as issue #128) still blocks `lipid-panel-ldl-question` from `verified`; category total unchanged at 4/12

Issue #113's live re-verification found `lipid-panel-ldl-question` reliably (10/10) surfacing and quoting the LDL reference-range guideline passage verbatim, with the extractor reliably attempting a citation for it — but misrouting it into `source_refs` as `tool_call_id="lipid-panel-reference"`, `record_id="lipid-panel-reference"` (the doc_id repeated in both fields, no section slug at all). Issue #85's existing `_coerce_misrouted_guideline_refs` coercion only reconstructed a chunk id from `tool_call_id="<doc_id>"` + `record_id="<section-slug>"`, so this shape fell through unhandled and the ref correctly failed `UNKNOWN_TOOL_CALL`. Issue #125 asked for a narrow extension recognizing this doc_id-doubled shape too.

**The fix.** `_coerce_misrouted_guideline_refs` (`services/copilot-agent/app/extraction.py`) now tries a second match alongside the original exact `f"{tool_call_id}#{record_id}"` chunk-id reconstruction: when `tool_call_id == record_id`, it looks up that value as a `doc_id` against the retrieved chunks, and coerces ONLY when it identifies **exactly one** retrieved chunk unambiguously (never guessing across more than one chunk sharing a doc_id — an adversarial regression test, `test_ambiguous_doubled_doc_id_with_multiple_matching_chunks_is_never_coerced`, locks this in). The pre-existing real-`call_<i>`-tool-call-id guard (`_REAL_TOOL_CALL_ID_RE`) is untouched and still checked first, so a genuine tool citation is never at risk. Two new regression tests were added to `services/copilot-agent/tests/test_extraction_guideline_ref_coercion.py`, red-first (both failed before the fix): one proving the doubled-doc_id shape now reconstructs and verifies end-to-end via `check_claims`, one proving an ambiguous multi-chunk doc_id collision is correctly left uncoerced (fails closed, same as a real hallucination).

**Live re-verification confirms the fix, at the extraction/provenance layer.** 18 fresh live draws total (10 full `evals.runner.pipeline.run_case` draws + 8 isolated extraction/provenance-focused draws, production `LlamaServerClient` against `development-easy-llama-server-1`, no replay, extraction calls at `temperature=0` and byte-identical every draw): 18/18 draws now produce a real, verbatim `guideline_chunk` `DocumentCitation` for the "considered high" claim that passes `check_document_citation` (`CitationStatus.VALID`) — the exact defect #125 was filed to fix no longer reproduces.

**But the case still does not reach `verified` — a separate, newly-discovered defect in the semantic-support judge layer, filed as issue #128.** All 10 full-pipeline draws land on `partially_verified`, 0/10 `verified`. Tracing why (8 additional isolated draws through `app.semantic_support.apply_semantic_support`) found the provenance-passing citation gets downgraded to `NOT_SEMANTICALLY_SUPPORTED` 8/8 times, with the live-captured judge reason: *"The QUOTE provides LDL cholesterol categories but does not specify the patient's LDL level, so it does not support the claim that the patient's LDL cholesterol level is considered high."* Root cause: the claim extractor splits the answer into two separate claims — one asserting the LDL value (172 mg/dL, citing the chart data) and one asserting it's "considered high" (citing the guideline categories chunk) — and `judge_support` evaluates the second claim's citation in total isolation, without the first claim's value. Read strictly alone, the "considered high" claim genuinely doesn't follow from a QUOTE that's just threshold ranges with no patient value attached — the judge's verdict is a reasonable one given the narrow context it's shown, not a flaky or wrong call. This is a claim-decomposition/judge-input-scoping gap in the `app.semantic_support` layer, unrelated to citation routing, and is out of #125's own scope — filed separately as **issue #128** rather than fixed here.

**No un-xfail.** `lipid-panel-ldl-question`'s `xfail` rationale has been rewritten (not removed) to describe this precisely: the #125 misrouting defect it previously described is now fixed and no longer the blocker, but issue #128's judge-scoping defect is. A fresh, currently-reproducible recording has been committed. **The `citation_present` category total is unchanged at 4/12.**

### Issue #118 follow-up: `metformin-renal-monitoring-question`'s #116-documented extraction-misattribution mechanism does not reproduce either — a THIRD, distinct mechanism blocks `verified`; category total unchanged at 4/12

Issue #116's "Issue #116 follow-up" section above documented `metformin-renal-monitoring-question` reaching `verified` 10/10 live draws, but for the wrong reason: the claim extractor allegedly never attached a real `DocumentCitation` for the guideline quote, instead duplicating an unrelated, trivially-valid chart-data `SourceRef` (the medication name) several times per guideline-derived claim — satisfying `check_claim`'s citation-validity AND-aggregation without ever citing the guideline. Issue #118 asked whether `check_claim`/`check_source_ref` needed a content-relevance check on ordinary `SourceRef`s to catch this.

**That #116 measurement turns out to have been taken with the issue #47/#81 semantic-support gate omitted** — an error in this investigation's own first repro attempt (a hand-reimplementation of `app.extraction.run_verification`'s steps that dropped the `apply_semantic_support` call), caught mid-investigation by re-checking the reimplementation against the real, unmodified `run_verification`. Once corrected, live re-verification (10 fresh draws, production `LlamaServerClient`, `evals.runner.pipeline.run_case`, no replay, the real unmodified pipeline including the semantic-support gate) shows a different picture entirely: **the extractor reliably attaches a real, verbatim `guideline_chunk` `DocumentCitation` (`renal-function-monitoring#metformin`) to the two claims that need one, 10/10 draws, and both pass the semantic-support judge ("supported").** `guideline_citation_present` — this eval category's actual regression target — **passes 10/10**. (The extractor does still emit 1-3 duplicate copies of the unrelated medication-name `SourceRef` alongside the real citation on the same claim; harmless, since the claim's `passed` status is grounded by the real `DocumentCitation` regardless.)

**The case still does not reach `verified`, though — for a THIRD, distinct mechanism.** The extractor reliably decomposes the answer into a 4th claim ("the exact frequency of kidney function checks should be determined based on the patient's individual health status and any changes in their condition") whose cited guideline passage ("...with dose adjustment or discontinuation considered at defined eGFR thresholds") does not actually support that specific generalization — an over-broad paraphrase, not a fabrication. The semantic-support judge correctly flags it `NOT_SEMANTICALLY_SUPPORTED`, 10/10 draws, stripping that one claim and dropping the overall verdict to `partially_verified`. This looks like the gate working as intended (catching a real over-claim, the exact failure mode it was built for per issue #47/#81), not a defect — **no fix was applied for it here.**

**Issue #118's own premise does not need fixing, either — it is filed as an open design question instead (issue #130), not a mandated fix.** `check_source_ref` validates structure (does the `tool_call_id`/`record_id`/`field` exist) and value (does `asserted_value` match the resolved raw value) only — it has no way to detect that a structurally-valid, value-matching `SourceRef` is irrelevant to what the claim's text actually asserts. This gap is real (a characterization test, `services/copilot-agent/tests/test_verification.py::test_source_ref_relevance_is_not_checked_structural_validity_only`, documents it directly), but with no live-reproducible case currently exercising it, forcing a broad relevance check onto every ordinary `SourceRef` in the suite — with real risk of false-rejecting genuinely-valid citations elsewhere — was judged not worth the risk per this issue's own conservative scope. Issue #130 tracks the open question for if/when a concrete motivating case appears.

**No un-xfail.** `metformin-renal-monitoring-question`'s `xfail` rationale has been rewritten again to describe this precisely: `guideline_citation_present` now passes reliably, but `verdict` does not (blocked by the semantic-support gate correctly catching an unrelated over-generalized claim), so the case as a whole still does not qualify for un-xfailing. A fresh, currently-reproducible recording has been committed. **The `citation_present` category total is unchanged at 4/12.**

### Issue #111/#128 findings: the semantic-support judge's per-citation isolation is fixed with an established-facts context — both cases now reach `verified`; category total moves to 6/12

Both issues trace to the same underlying gap: `app.semantic_support.judge_support`/`apply_semantic_support` only ever receives ONE claim's isolated text plus ONE citation's quote — it has no visibility into that claim's OTHER citations, or into sibling claims from the same answer that already establish a fact the current claim's assertion depends on.

- **#111** (`bp-stage2-question`): a single claim carries TWO citations on the SAME claim — a chart-data `SourceRef` for the patient's actual 148/94 mmHg reading, and a `guideline_chunk` `DocumentCitation` categorizing that reading as "Stage 2 hypertension." The category-threshold guideline excerpt never itself restates a specific patient value (it can't — it's a reference document), so judging it in isolation reasonably (but wrongly, given the full picture) concludes it doesn't support the claim.
- **#128** (`lipid-panel-ldl-question`): the SAME underlying gap, across two SEPARATE claims instead of one — a chart-value claim ("...LDL cholesterol level is 172 mg/dL," citing a `SourceRef`) and a category claim ("...is considered high," citing a `guideline_chunk` `DocumentCitation`). The second claim's judge call never sees the first claim's value.

**The fix** (`services/copilot-agent/app/semantic_support.py`): before judging a `DocumentCitation`, `apply_semantic_support` now gathers every already-established `SourceRef` fact available to that citation's claim — (a) the claim's OWN `SourceRef`s (already deterministically re-validated against the raw record — `check_source_ref`, no LLM judgment involved), plus (b) every SIBLING claim's `SourceRef`s, but ONLY for a sibling whose citations are entirely `SourceRef`s (no `DocumentCitation`s of its own) and which has already fully `.passed` — and hands that list to the judge as an additional ESTABLISHED FACTS block in the prompt. A category/threshold guideline quote can now be judged against the specific value it categorizes, even when the quote itself never restates that value.

**Explicit safety invariant (never relaxed, enforced in code and tested):** only facts already confirmed by deterministic, LLM-free re-validation (`CitationStatus.VALID` `SourceRef`s) are ever surfaced as "established." A sibling claim that failed provenance, or whose own fact still depends on an as-yet-unjudged `DocumentCitation`, is excluded from context entirely — the judge is never handed a fabricated, invented, or merely-asserted-but-unconfirmed fact, only values already proven byte-for-byte against the same conversation's raw tool results. `DocumentCitation` quotes are deliberately never used as established-facts context either (even when provenance-`VALID`): their "fact" is exactly what semantic support is judging, so treating one as settled ground truth for another judge call would be circular. Four structural tests (`services/copilot-agent/tests/test_semantic_support.py`) lock in both target shapes plus both exclusion rules (failed sibling, sibling with its own `DocumentCitation`).

**Red-first:** two tests reproducing the exact #111/#128 shapes were written and confirmed failing (the judge prompt lacked the sibling value) before the fix; both pass after.

**Live re-verification** (production `LlamaServerClient`, `evals.runner.pipeline.run_case`, no replay):
- `bp-stage2-question`: **25/25 verified** across two independent multi-draw sessions (10 + 15 draws), plus **4/4 more** via `evals/runner/record.py` itself (the actual recording tool) once its container's directory layout was corrected (see caveat below).
- `lipid-panel-ldl-question`: **25/25 verified** across two independent multi-draw sessions (10 + 15 draws), plus **4/4 more** via `record.py`.
- **Investigation caveat, disclosed for completeness:** early in this investigation, three consecutive `record.py` invocations (from a throwaway container mounting only `services/copilot-agent/app/` and `evals/`, not preserving their real sibling directory relationship) showed `partially_verified` instead. Root-caused: `record.py`'s own path derivation (`_AGENT_ROOT = _EVALS_ROOT.parent / "services" / "copilot-agent"`) assumes `evals/` and `services/copilot-agent/` are siblings under one repo root; the misconfigured container broke that assumption, so `record.py` silently fell back to a **stale, pre-fix** copy of `app/semantic_support.py` baked into the image's `site-packages` (a non-editable `pip install .` artifact) instead of the locally-edited code. This was a test-harness-container bug, not flakiness in the fix or the model — confirmed by (a) direct calls to the real `runner.pipeline.run_case`/`runner.record.record_case` functions from a correctly-pathed script succeeding throughout, even interleaved with the failing script-invocations, and (b) `record.py` succeeding reliably (4/4) once the container's mount layout was corrected to match the real repo's directory structure.
- **`statin-liver-monitoring-question`** (bonus candidate per the prior session's assessment) was spot-checked (6 draws) and **did not flip** — it remains `partially_verified` for a genuine, unrelated reason: the guideline quote says routine liver-enzyme monitoring is usually NOT needed for an asymptomatic patient, while the claim asserts it IS generally recommended — a real direction mismatch (issue #47's original target failure mode), not a context-scoping gap. Left alone, per this session's conservative posture.

**No fabrication risk introduced.** The judge only ever sees genuinely-established facts already proven against the SAME answer's own raw tool results — never an invented, guessed, or another-answer's fact.

**Both `bp-stage2-question`'s and `lipid-panel-ldl-question`'s `xfail` markers have been removed**, with fresh `verified` recordings committed for each. **The `citation_present` category total moves from 4/12 to 6/12** — `a1c-target-question`, `lithium-nsaid-question`, `warfarin-antibiotic-question`, `statin-ck-myopathy-question`, `bp-stage2-question`, and now `lipid-panel-ldl-question`.

### Issue #133 findings: `nsaid-ace-inhibitor-question` un-reproduces and un-xfails; `renal-function-ace-question` un-reproduces its old rationale but finds a different, genuine blocker — category total moves to 7/12

Issue #133 was raised from a STALE, pre-#85/#105/#108/#111/#128 recording read: it hypothesized a possible "5th SourceRef-misrouting shape" (`tool_call_id=doc_id` + `record_id=full-chunk-id`, doc_id duplicated inside the reconstructed section id) blocking both `nsaid-ace-inhibitor-question` and `renal-function-ace-question`, explicitly flagged as unconfirmed against current `main`.

**Method.** Both cases were re-run live, 8 fresh draws each, through the real pipeline (`evals.runner.pipeline.run_case`, production `LlamaServerClient`, no replay, inside `development-easy-agent-1` against `development-easy-llama-server-1`) — the same rigor as #100/#108/#109/#113/#125/#118. Per this issue's own instruction, every draw's `document_citations`/claim structure was inspected directly (not just the overall verdict).

**`nsaid-ace-inhibitor-question`: genuinely un-xfailed, no code change needed.** The hypothesized 5th misrouting shape was never observed — 8/8 draws attach a correctly-shaped `document_citations` entry (`nsaid-interactions#ace-inhibitors-and-arbs`) directly from extraction, with no stray `source_refs` entry needing coercion. The OLD xfail rationale (chart-data-only answering; 3/6 claims stripped for a missing source ref) also does not reproduce — every claim in every draw carries a valid citation, and the guideline citation passes both provenance re-validation AND the semantic-support judge, 8/8. `verified` 8/8. Its `xfail` marker has been removed and a fresh recording committed (first re-record attempt landed on `verified`, consistent with the fully reliable outcome, not cherry-picked).

**`renal-function-ace-question`: OLD rationale also does not reproduce, but a different, genuine mechanism blocks it — remains honestly `xfail`.** The hypothesized 5th misrouting shape was again never observed (no misrouted `source_refs` in any draw), and the OLD "unrelated chart-data claim stripped for a missing source ref" rationale does not reproduce either — every claim in every draw carries a valid citation. `guideline_citation_present` now passes reliably, 8/8. But `verdict` reaches `verified` in only 1/8 draws; the other 7/8 land on `partially_verified`. Root cause: the model's answer splits the guideline's monitoring recommendation across two claims, and the claim asserting the recheck timing ("A recheck is typically recommended at roughly 1-2 weeks after starting or up-titrating the medication") quotes only the guideline's second clause ("with a recheck at roughly 1-2 weeks after starting or up-titrating") — the first clause naming WHAT is being rechecked (creatinine/eGFR/potassium) is left out of the quoted span. The issue #47/#81 semantic-support judge, given only that claim text, that truncated (but genuinely verbatim, provenance-valid) quote, and this claim's own established chart facts (just the medication name/status, no kidney-function fact), correctly cannot confirm support and downgrades it to `NOT_SEMANTICALLY_SUPPORTED`, 7/8 draws. This is the judge doing its job on a citation that is real but was quoted incompletely by the model — the same general shape as issue #118's "gate correctly catching an under-supported claim," not a defect, so per this issue's own conservative instruction no fix is forced here. (The 1/8 outlier draw phrased the claim as "A recheck of kidney function should be done ...", naming the antecedent explicitly, and passed — ordinary wording variance, not evidence of a different mechanism; `LlamaServerClient` defaults `temperature: 0`, so this variance reflects llama.cpp's known non-strict argmax determinism under batching, not true sampling.) The committed recording captures the typical (7/8) `partially_verified` outcome — 3 consecutive re-record attempts all landed on it, so it is not a cherry-picked draw. Its `xfail` rationale has been rewritten (not removed) to describe this precisely.

**No production code changed.** Neither case ever exhibited a citation-routing shape for `_coerce_misrouted_guideline_refs` to recognize — issue #133's own hypothesis does not hold for either case, confirmed live rather than assumed from the stale recording.

**The `citation_present` category total moves from 6/12 to 7/12** — `a1c-target-question`, `lithium-nsaid-question`, `warfarin-antibiotic-question`, `statin-ck-myopathy-question`, `bp-stage2-question`, `lipid-panel-ldl-question`, and now `nsaid-ace-inhibitor-question`.

### Issue #123 findings: planner tool-dispatch hardening for `dual-antiplatelet-question`/`hypertension-lifestyle-followup-question` — confirmed live, a fix was implemented and live-verified, but declined after the mandatory 10-case regression check found it degrades an unrelated, previously-reliable case; category total unchanged at 7/12

Issue #123's root cause (from issue #115) was re-confirmed live before any change: for both cases, the planner never dispatches the tool that would confirm a chart fact the question already states in words — `dual-antiplatelet-question`'s trace is `[get_problems, get_allergies]` (`get_medications` never called), `hypertension-lifestyle-followup-question`'s is `[get_appointments, get_encounters]` (`get_vitals` never called). With no real tool-result record backing the stated fact, the extractor fabricates a plausible-looking `SourceRef` that `check_source_ref` correctly rejects, so both land on `blocked` (`NONE_VERIFIED`) regardless of how clinically accurate the answer is. Reproduced deterministically, byte-identical (`temperature=0`), matching #115's account exactly.

**The fix attempted.** Direction 2 (extraction-prompt hardening to prefer an uncited claim over a fabrication) was re-confirmed, by re-reading #115's own analysis, to be structurally incapable of reaching `verified` for either case even if implemented perfectly: `compute_verdict` requires ALL claims to pass (`_CitationState.ALL_VERIFIED`), and each case has a MAJORITY of claims independently fabricated with no fallback citation — an uncited claim still fails `.passed` exactly like a fabricated one (`app.schemas.verification.Claim`'s own docstring: `passed` requires `bool(citation_results)`, so zero citations is not a free pass). So only direction 1 (planner tool-dispatch hardening) can possibly work, and it was implemented: one additive Rules-bullet in the planner's system prompt ("if the question already states a chart fact, still call the tool that would confirm it") plus one additive medications-domain few-shot example (worded differently from `dual-antiplatelet-question` itself, to avoid literally training the fix onto the eval question). Purely additive, no in-place rewording — the lower-risk shape per this project's own P3G.1b lesson.

**Target-case live verification.**
- `dual-antiplatelet-question`: **8/8 fresh draws reach `verified`**, `get_medications` genuinely dispatched every time, and the claim structure is genuinely, meaningfully grounded on inspection — `field='name'`/`'status'` citations directly backing "taking aspirin"/"taking clopidogrel," plus the same real, verbatim guideline quote #115 already found. A clean, honest fix for this case in isolation.
- `hypertension-lifestyle-followup-question`: a matching vitals-domain few-shot example (mirroring the medications one, for `get_vitals`) was tried FIRST and produced a worse failure mode, caught before it was ever considered for shipping: 8/8 draws reached `verified`, but the planner still never called `get_vitals` — it added a `get_patient_summary` call instead (record COUNTS, not the vitals reading), and the extractor cited `get_patient_summary`'s `problem_count=0` field for the claim "blood pressure was elevated." That field/value pair is real and passes `check_source_ref`'s deterministic equality check purely by coincidence — it has zero topical connection to blood pressure, and `apply_semantic_support`'s LLM judge does not catch it either, since that judge only re-judges `document_citations`, never plain `source_refs` (by design — see `app.verification`'s module docstring on why `SourceRef` checks are deterministic value-equality only). This is a **false-positive `verified`**, arguably worse than the original fail-closed `blocked`. Removing just that vitals-domain example (keeping the medications one and the general Rules bullet, which do not by themselves induce the `get_patient_summary` substitution) reverted this case cleanly back to its original, safe `blocked` behavior, 3/3 fresh draws — no false positive, no change from its pre-fix state. **The vitals half was declined on this evidence alone**, before the fix was ever considered complete; only the medications-domain half proceeded to the mandatory regression check.

**Mandatory 10-case regression check — this is where the medications-only fix was also declined.** Per this issue's own instruction, all 10 other `citation_present` cases were live-verified (4 fresh draws each) with the medications-only fix applied, inside the same session. Compared directly against a same-session baseline (the exact same 10 cases, 4 draws each, against unmodified `main`'s `planner.py`, run immediately after, same model/container state):

| Case | Baseline (main) | With fix | 
|---|---|---|
| `a1c-target-question` | 0/4 verified | 0/4 verified (unchanged) |
| `bp-stage2-question` | 3/4 verified | 0/4 verified |
| `lipid-panel-ldl-question` | 0/4 verified | 0/4 verified (unchanged) |
| `lithium-nsaid-question` | 4/4 verified | 0/4 verified |
| `metformin-renal-monitoring-question` (xfail) | 3/4 verified | 0/4 verified |
| `nsaid-ace-inhibitor-question` | 1/4 verified | 0/4 verified |
| `renal-function-ace-question` (xfail) | 4/4 verified | 0/4 verified |
| `statin-ck-myopathy-question` | 4/4 verified | 0/4 verified |
| `statin-liver-monitoring-question` | 4/4 verified | 4/4 verified (unchanged) |
| `warfarin-antibiotic-question` | 4/4 verified | 4/4 verified (unchanged) |

Several of these differences are plausibly just this project's own well-documented GPU-level sampling non-determinism (`temperature=0` does not guarantee byte-identical output across all calls on this CUDA setup — see issue #113's reranker-score-variance finding): a follow-up re-confirmation of `statin-ck-myopathy-question` under the SAME fix, in a second independent batch, landed on **4/4 `verified`** (not 0/4) — clearly noise, not a real effect of the fix, for that specific case.

**`lithium-nsaid-question` is the one solid, load-bearing finding, and it is enough on its own to decline the fix.** Two independent confirmation batches under the medications-only fix BOTH landed on **4/4 `partially_verified`**, byte-identical both times, versus **4/4 `verified`** at the same-session baseline — a clean, reproduced flip, not noise. Critically, the planner's tool-dispatch TRACE is identical between baseline and fix (`['get_allergies', 'get_medications', 'get_problems']` both times) — the regression is not in which tools ran, it is in the model's final answer composition and the extractor's claim decomposition, both of which read the FULL system prompt (including the newly-added, medications-domain-scoped Rules bullet and few-shot example) on every turn of every case, whether or not that addition is topically relevant to the question being asked. Direct inspection of the claim structure shows two of the four claims newly fail on this case with the fix applied (the case's own chart-data claims picking up citations to the wrong record fields), a small but real ripple effect from a prompt addition that was scoped in INTENT (medications-only) but not scoped in EFFECT (the system prompt is one shared string every question sees).

**Decision: declined, no production code shipped.** This is exactly the risk this project's own history (`app.extraction._GUIDELINE_INSTRUCTIONS`'s P3G.1b comment) already warned about for extraction-prompt changes, now confirmed to apply to PLANNER-prompt changes too, and to apply even to a purely ADDITIVE change (not just an in-place rewording) — on an 8B model, any change to a prompt shared across every question can measurably shift unrelated, previously-reliable behavior. Shipping a fix that reliably helps 1 case (`dual-antiplatelet-question`, +1) at the cost of reliably breaking another previously-verified case (`lithium-nsaid-question`, -1) is not a net-safe change under this project's own conservative bar — net effect on `citation_present` would be zero at best, with the actual harm (a previously-trustworthy case becoming unreliable) landing on a real case a clinician could see today, not a hypothetical one. Per issue #123's own explicit instruction to decline rather than force a fix under exactly this evidence pattern: **planner.py is unchanged, `hypertension-lifestyle-followup-question` and `dual-antiplatelet-question` both remain `xfail`, and `citation_present` remains 7/12.**

**What would be needed to revisit this safely (flagged for the maintainer, not attempted here):** a much larger live regression sample per case (4 draws is enough to catch `lithium-nsaid-question`'s clean flip, but not enough to rule out noise on borderline cases like `statin-ck-myopathy-question`) and/or a turn-scoped hint mechanism that only activates for a question actually matching the "states a chart fact directly" heuristic, rather than a permanent addition to the shared system prompt every question sees regardless of relevance — the latter is a larger architecture change (conditionally composing the system prompt per-turn) outside this issue's own scope.

### Issue #130 findings: SourceRef relevance-gate measurement spike — downgrade criterion fires; gate deferred, unchanged verdicts, `citation_present` remains 7/12

**Why this was measured.** `app.semantic_support`'s LLM judge (issue #47/#81) only re-judges `DocumentCitation`s (quote-based citations) for semantic support — a plain `SourceRef` citation is checked only for deterministic value-equality (`check_source_ref`), never for topical relevance. Issue #123's investigation (above) reproduced, live, exactly the shape this gap allows: a claim "blood pressure was elevated" paired with a real, provenance-valid but topically irrelevant `problem_count=0` `SourceRef` — a coincidental-match false positive the existing judge cannot catch by design. That was on a declined, off-protocol variant (proof the shape is reachable, not a standing live defect), and issues #86/#70 have since widened the `SourceRef` surface to ingested document facts, growing the exposure. Per the ADR (recorded 2026-07-21): **measure first, do not add a relevance gate to the verdict path yet.** This section records that measurement. No production code changed; no verdict changed for any case.

**Pre-registered criteria (from the ADR, verbatim).** Upgrade (enable enforcement) requires ALL of: shadow false-reject rate on currently-passing claims effectively zero across ≥8 draws/case; positive control caught; a live-reproducible on-protocol occurrence found OR census shows material exposure. **Downgrade: any false-reject on a valid terse chart citation → the gate is unsafe at 8B judge reliability and the documented gap stands.**

**Deliverable 1 — offline structural census** (`evals/runner/census_source_ref_claims.py`, deterministic, no LLM call, walks the 65 committed `evals/recordings/*.json` files). Of 85 total claims across every recording, **62 (73%)** are grounded ENTIRELY by `SourceRef`s (zero `DocumentCitation`s) — the unjudged-relevance exposure surface. Selected rows (full table reproducible via the script; every recording with at least one claim is a nonzero row):

| Case | Total claims | SourceRef-only (exposure) |
|---|---|---|
| `nsaid-ace-inhibitor-question` | 6 | 6 |
| `hypertension-lifestyle-followup-question` | 6 | 5 |
| `factually-consistent-anticoagulant-doac` | 4 | 4 |
| `factually-consistent-medication-dose-fact` | 4 | 4 |
| `warfarin-aspirin-major-interaction` | 4 | 4 |
| `compound-medication-allergy-conflict` | 3 | 3 |
| `factually-consistent-lipid-ldl-optimal` | 3 | 3 |
| `p32-redacted-freetext-still-verifies` | 3 | 3 |
| `bp-stage2-question` | 3 | 2 |
| `dual-antiplatelet-question` | 5 | 2 |
| `warfarin-antibiotic-question` | 6 | 2 |
| **Total (all 65 recordings)** | **85** | **62** |

Material exposure, by inspection: the third upgrade-criterion clause ("census shows material exposure") holds. Every other upgrade clause is evaluated against the live spike below.

**Deliverable 2 — live shadow-judge spike** (`evals/runner/issue_130_spike.py`, standalone, not wired into `run_verification` or any production path). Reused `app.semantic_support`'s judge plumbing (`SemanticSupportJudgeLike.extract`, `SemanticSupportJudgement`/`SupportVerdict`, fail-closed on `LLMEngineError`) with a NEW, SourceRef-oriented prompt (facts as `field: value` pairs, not a quote). Ran all 12 `citation_present` cases live (real `LlamaServerClient` calls against the pinned 8B-Q5 model, no replay), 8 draws each, scoped exactly to claims whose surviving citations are all `SourceRef`s (the census's exposure population) — one shadow judge call per such claim, per draw, logging a would-downgrade decision without ever touching the actual `ClaimCheckResult`/verdict. 104 total draws (96 case-draws + 8 positive-control draws), zero pipeline exceptions.

**Positive control.** The reconstructed issue #123 shape — claim `"The patient's blood pressure was elevated."` paired with `SourceRef` fact `problem_count: 0` — was fed to the same judge call 8 times. **Caught 8/8 (100%)**, every time with a reason correctly identifying the fact as topically unrelated to blood pressure (e.g. *"The SOURCE FACTS provide information about the number of active problems, which is unrelated to the patient's blood pressure."*). This upgrade-criterion clause holds cleanly.

**False-reject rate on currently-passing claims — this is where the downgrade criterion fires.** Of 144 shadow-judged claims (every one already scored `passed` by the live pipeline before the shadow judge ever ran), **33 would have been downgraded (false rejects)** — concentrated entirely in two cases:

| Case | Judged claims | Would-downgrade (false rejects) |
|---|---|---|
| `statin-ck-myopathy-question` | 32 | 24 |
| `statin-liver-monitoring-question` | 25 | 9 |
| All other 10 `citation_present` cases (combined) | 87 | 0 |

Direct inspection of the false-reject records shows a consistent, reproducible shape, not noise: both cases decompose one medication fact into several single-field claims per draw (`"...taking Simvastatin."` / `"...at a dose of 40mg."` / `"...orally."` / `"...currently on active medication..."`), each carrying only ONE bare `SourceRef` field (`name: Simvastatin`, `dose: 40mg`, `route: oral`, `status: active`) with no sibling context in the judge call. The judge reliably marks the terse, single-field claims `not_supported` — e.g. for `route: oral` against `"The patient is taking Simvastatin orally."`: *"...only indicates the route of administration but does not specify the medication name, so it does not confirm the patient is taking Simvastatin."* Every one of these citations is a genuinely valid, correctly-attributed chart fact — exactly the "valid terse chart citation" the downgrade criterion names, because the SourceRef-oriented judge (unlike `apply_semantic_support`'s established-facts context, issues #111/#128) was scoped to this spike's own claim-only prompt with no sibling-fact context wired in.

**Honest conclusion: gate deferred, per the ADR's own pre-registered downgrade criterion.** The positive control is caught cleanly and the census shows material exposure, but the false-reject criterion is the one that must hold for enablement, and it does not: 33/144 (23%) of currently-passing claims would be wrongly downgraded (see the per-case breakdown above). Per the ADR, this is decisive on its own — **the gate is unsafe to enable at 8B judge reliability with this prompt shape, and the documented gap (SourceRef relevance is checked for provenance only, never semantics) stands, undefended.** No verdict changes as a result of this measurement; `citation_present` remains 7/12. Reopening requires the false-reject rate to actually reach zero (e.g. an established-facts-context fix mirroring #111/#128's fix for `DocumentCitation`s, then re-measured) — not attempted here, this issue's scope was measurement only.

Artifacts: `evals/results/issue-130/draws/` (104 per-draw JSON files, incremental — a crash would have lost at most one draw) and `evals/results/issue-130/summary.json` (the aggregation above, reproducible via `python evals/runner/issue_130_spike.py --summarize-only`).

## Measured mid-tier addendum (RTX 3060 12GB desktop)

**This section is measured mid-tier evidence, not a minimum-spec update.** Every
number below carries the label **measured mid-tier (RTX 3060 12 GB desktop)**
and must never be blended with the RTX 5060 Laptop 8 GB numbers above — same
convention `docs/W2_ARCHITECTURE.md` uses for its own recommended-tier VLM
figures (stated directional/measured separately, never averaged into the
minimum-spec baseline). The hardware was a separate desktop-class machine
(12 GB VRAM, 128 GB system RAM); no local file paths or operator-machine
details are reproduced here beyond that.

**Fresh draw vs. stable recording, again.** The (now-superseded, see issue
#100 above) 5/12 baseline was a re-judge of stable, already-committed answer
recordings — deliberately insulated from planner/extraction variance so it
was a reproducible number the P3G.2 gate could guard. Every number in this
addendum is instead a **fresh single draw** of the full pipeline (planner →
extraction → provenance → semantic-support judge) on the desktop, run once
per case with no re-rolls — the same *kind* of measurement issue #100 later
ran repeatedly (8-10 draws/case) on the laptop tier and found mostly failing.
The two measurement styles answer different questions and are not
apples-to-apples with each other, exactly as already disclosed for the 14B
run below.

### Results table

| Model / config | Params / quant | citation_present | Mean latency | Extraction errors | VRAM / RAM |
|---|---|---|---|---|---|
| Qwen3-8B-Q5_K_M (pre-fix, 4 fresh draws) | 8B, Q5_K_M | **2/12** (identical on all 4 draws) | 26.0–30.8 s | 2–3 / 12 | 7.6 GB VRAM |
| Qwen3-14B-Q4_K_M | 14B, Q4_K_M | 4/12 | 33.3 s | 2 / 12 | 10.7 GB VRAM |
| Qwen3-14B-Q5_K_M (issue #89, 1 fresh draw) | 14B, Q5_K_M | 3/12 | 38.0 s | not tracked in this comparison — see "Issue #89 findings" below | ~11.9 GB VRAM (212 MiB headroom) |
| Qwen3-30B-A3B (`--n-cpu-moe 32`) | 30B MoE, Q5_K_M | 3/12 | 57.6 s | 2 / 12 | 9.9 GB VRAM |
| Qwen3.6-35B-A3B (`--n-cpu-moe 30`) | 35B MoE, Q5_K_M | 5/12 | 81.9 s | 3 / 12 | 10.3 GB VRAM |
| gpt-oss-120b (`--n-cpu-moe 32`) | 120B MoE, Q8_K_XL | 5/12 | 220.6 s | **0 / 12** | 10.5 GB VRAM + ~60 GB CPU RAM |
| Qwen3-8B-Q5_K_M (post-fix, 1 fresh draw) | 8B, Q5_K_M | **4/12** | 25.6 s | 0 / 12 | 7.6 GB VRAM |

All rows use the same 12 `citation_present` eval cases, the same tightened
verified-citation bar (provenance AND semantic support), and the same engine
self-judging pattern as the committed baseline's methodology.

### The determinism finding

Four independent fresh draws of the production 8B-Q5 model, back-to-back on
this desktop (temp 0, `--parallel 1`, no sampling stochasticity), landed on
**exactly 2/12 every time** — the same 2 cases passed and the same 2–3 cases
hard-errored on extraction in each draw. This is a materially different
finding from the 1/12–6/12 scatter this document already discloses for
fresh full-pipeline re-draws on the laptop, and it is stated separately
rather than folded into that variance language: on this hardware/config,
repeat draws were not noisy, they were flatly reproducible at a number
*below* the committed 5/12 (which, as already explained above, is a
stable-recording re-judge, not a fresh draw — the two were never measuring
the same thing).

After PR #94/#95 merged a pipeline fix (`LlamaServerClient`'s
claim-extraction call given its own 2560-token budget, separate from chat's
1536-token budget, fixing a real truncation bug), one further fresh 8B-Q5
draw scored **4/12 at 25.6 s mean — faster and better than every pre-fix
draw**. The causal story behind that jump is not yet fully closed: 4 cases
changed outcome between the pre-fix and post-fix draws (3 improved, 1
regressed), where only 1 case had been predicted to flip from the
token-budget diagnosis alone. Issue #93 (planner tool-call nondeterminism, a
separate uncitable-claim-handling behavior) remains open and may explain
further movement in these numbers.

### Honest verdict

**No model swap tested here beat the pipeline-fixed 8B-Q5 result.** The
single most effective intervention measured this session was a bug fix, not
a bigger model — 14B (Q4_K_M and Q5_K_M — see "Issue #89 findings" below),
30B-A3B, 35B-A3B, and gpt-oss-120b all either scored
lower or cost dramatically more latency (up to 9x) than the post-fix 8B-Q5's
4/12 at 25.6 s, and none reach the committed baseline's 5/12 without paying
a latency penalty that disqualifies them for interactive use. gpt-oss-120b
is the one model with zero extraction errors in this series, but its 220.6 s
mean is 7–8x over the latency envelope this project targets, disqualifying
it despite that reliability signal. The 30B-A3B result (3/12, 57.6 s, same
extraction-error signature as its original laptop rejection) reproduces the
prior rejection's failure mode on materially better hardware — this is
**confirmatory evidence for that decision**, not new information
contradicting it (see "The ladder actually tested" above).

This addendum is not a final verdict on model selection: issue #93 is open,
and these numbers may move again once it resolves. It also does not cover
the VLM/document-ingestion path — a separate Q8_0 vision-fabrication retest
(issue #90) is out of scope here and, if its result isn't already reflected
in `docs/W2_ARCHITECTURE.md`'s vision-tier section, is a follow-up for that
document, not this one.

### Issue #89 findings: Qwen3-14B-Q5_K_M vs. same-day 8B-Q5 control — 3/12 vs. 4/12, no meaningful improvement

Issue #89 asked for a **different quantization** of the 14B candidate than the
one already measured in this addendum's results table above
(`Qwen3-14B-Q4_K_M`, 4/12, from the earlier same-session sweep): the actual
GGUF pinned for this issue is **`bartowski/Qwen_Qwen3-14B-GGUF` @
`bd080f768a6401c2d5a7fa53a2e50cd8218a9ce2`, `Qwen_Qwen3-14B-Q5_K_M.gguf`
(10,514,570,272 bytes, size+sha256 verified before this run)** — a full
grade heavier than the Q4_K_M row already in the table. This section is a
fresh, honest measurement of that specific file, run through the same
harness discipline this addendum already established (single fresh draw per
case, no re-rolls, `evals.runner.pipeline.run_case`/production
`LlamaServerClient`, `result.rendered.segments`/`document_citations`
inspected directly per case rather than trusting the top-level `verdict`
string alone — issue #109's own lesson).

**Method.** Both rows below are same-day, same-session, same-hardware draws
(this desktop, RTX 3060 12 GB) captured back-to-back in one sitting so
neither draw's answer is stale relative to the other: (1) a fresh 8B-Q5
CONTROL — the exact serving config already committed for this hardware tier,
restarted clean before the run — over all 12 `citation_present` cases, one
draw each; (2) a docker-managed swap to the 14B-Q5_K_M file, one draw each
over the same 12 cases, identical draw protocol; (3) a docker-managed swap
back to 8B-Q5, byte-identical to its pre-swap `docker inspect` image digest
and `Cmd`, health-checked and smoke-tested before being called done. Per-case
JSON artifacts (verdict, claim/segment structure, `document_citations`,
latency) for both runs are committed under
`evals/results/issue-89/{8b-q5,14b-q5}/`.

**Exact serving configs (both engines, identical apart from `--model` and
container name):**

```
image: ghcr.io/ggml-org/llama.cpp:server-cuda-b10068@sha256:b8791d2ab8c51570e75067f626db7a271caa3e99396056f2721fac432e03c852
--model <qwen3-8b-q5_k_m.gguf | Qwen_Qwen3-14B-Q5_K_M.gguf>
--ctx-size 16384 --parallel 1
--cache-type-k q8_0 --cache-type-v q8_0 --flash-attn on
--n-gpu-layers 999 --host 0.0.0.0 --port 8080 --reasoning off
```

8B-Q5 is the compose-managed `development-easy-llama-server-1` container,
unmodified. 14B-Q5_K_M was served from a standalone `docker run` (same image
digest, same flags, host-mounted GGUF, attached to the compose
`copilot_internal` network under its own alias) rather than editing the
compose file, so the swap is fully reversible with no compose-file diff —
per this issue's own "docker-managed swaps only" instruction. `ollama` and
`llama-server-embed` were stopped for the duration of both runs to keep the
GPU-memory ledger clean (neither is on this eval harness's call path: cases
inject their `retrieved_chunks` fixture directly rather than calling live
retrieval/embedding).

**Hardware-fit note (issue #89 flagged this as a real risk, and it materialized).**
The desktop's GPU already carries a **fixed ~5.4 GB non-container floor**
(Windows desktop compositor/background apps — confirmed by `nvidia-smi`
before either engine loaded any weights, and unaffected by stopping every
project container). That leaves roughly **6.7 GB, not 12 GB,** actually
available to an engine. 14B-Q5_K_M (10.5 GB on disk) does not fit inside
that reduced budget: `llama-server`'s own auto-fit calculator logged
`failed to fit params to free device memory: n_gpu_layers already set by
user to 999, abort` at load time. Despite that warning, the load *did*
eventually complete fully GPU-resident (all layers) after roughly 2.5
minutes — reported final usage 11,902 MiB / 12,288 MiB (**212 MiB free**),
smoke-tested with a real completion call before the measurement pass began.
This is recorded plainly as an anomaly, not silently smoothed over: the
requested config (`--n-gpu-layers 999`, full residency) nominally should not
have fit this machine's real available budget, and did anyway, with almost
no headroom left — a materially more fragile state than 8B-Q5's comfortable
~7.6 GB/12 GB fit. No partial CPU offload was needed in the end, but the
margin was thin enough that a slightly larger KV allocation or a second
concurrent GPU consumer could plausibly have OOM'd mid-run; this should be
read as "it fit, barely, this time," not "this config reliably fits this
hardware."

**Results.**

| Case | 8B-Q5 (control, fresh draw) | 14B-Q5_K_M (fresh draw) |
|---|---|---|
| `a1c-target-question` | `verified` (16.6 s) | `partially_verified` (43.2 s) |
| `bp-stage2-question` | `partially_verified` (16.8 s) | `partially_verified` (31.4 s) |
| `dual-antiplatelet-question` | `blocked` (38.0 s) | `blocked` (30.6 s) |
| `hypertension-lifestyle-followup-question` | `blocked` (21.4 s) | `blocked` (19.1 s) |
| `lipid-panel-ldl-question` | `partially_verified` (13.3 s) | `partially_verified` (21.3 s) |
| `lithium-nsaid-question` | `verified` (22.1 s) | `partially_verified` (34.1 s) |
| `metformin-renal-monitoring-question` | `partially_verified` (30.8 s) | **`verified`** (41.1 s) |
| `nsaid-ace-inhibitor-question` | `partially_verified` (39.2 s) | `partially_verified` (61.7 s) |
| `renal-function-ace-question` | `partially_verified` (27.9 s) | **`verified`** (40.7 s) |
| `statin-ck-myopathy-question` | `partially_verified` (40.0 s) | `partially_verified` (36.0 s) |
| `statin-liver-monitoring-question` | **`verified`** (58.9 s) | `partially_verified` (44.7 s) |
| `warfarin-antibiotic-question` | `verified` (39.1 s) | `verified` (52.6 s) |
| **Total verified** | **4/12** | **3/12** |
| **Mean latency** | **30.3 s** (13.3–58.9 s) | **38.0 s** (19.1–61.7 s) |

Every "verified" cell above was confirmed with the same segment-inspection
discipline already established above (issue #109's lesson): each `verified`
case genuinely carries a `source_type == "guideline_chunk"` `DocumentCitation`
that passed both provenance re-validation and the semantic-support judge;
none is a verdict-only false positive.

**This is a same-day single-draw comparison, not a large-sample one** — see
"Fresh draw vs. stable recording, again" above for why these fresh
single-draw numbers are a different measurement regime from the committed,
stable-recording `citation_present` baseline (issue #133: 7/12), and are
instead a fresh single draw per case on each engine, exactly matching this
addendum's own already-disclosed methodology and its own already-disclosed
1/12–6/12 fresh-draw variance band for this pipeline. Three cases (`a1c-target-question`,
`lithium-nsaid-question`, `statin-liver-monitoring-question`) flipped from
`verified` on 8B-Q5 to `partially_verified` on 14B-Q5_K_M; two
(`metformin-renal-monitoring-question`, `renal-function-ace-question`)
flipped the other way. Net: one fewer `verified` case on the bigger model,
well within the single-draw noise this pipeline is already known to produce
on either engine — not treated here as evidence that 14B is *worse* at
citing, only as evidence that a single draw on either engine cannot
distinguish "genuinely better," "genuinely worse," and "noise" from each
other.

**Honest verdict: 14B-Q5_K_M does not meaningfully improve citation quality
over 8B-Q5 on this hardware, and costs meaningfully more latency for it.**
3/12 vs. 4/12 is a **decrease**, not an improvement — this result alone
would not justify adopting 14B even setting latency aside. Mean latency is
**25% higher** (38.0 s vs. 30.3 s), and the hardware-fit red flag above is a
real operational concern independent of the citation numbers. Combined with the
Q4_K_M row already in this addendum's results table (4/12, matching 8B-Q5
exactly, not beating it either), **no quantization of Qwen3-14B measured
across either issue #89 or the earlier sweep beats 8B-Q5 under this
addendum's fresh-draw protocol on this hardware** (a separate measurement
regime from the committed, stable-recording `citation_present` baseline —
see "Fresh draw vs. stable recording, again" above and issue #133's 7/12).
Per issue #89's own "Done when" criterion
("if meaningfully better, presented ... as a candidate measured mid-tier
model"): **it is not meaningfully better, and is not presented as a
candidate for the mid-tier doc addendum.** The maintainer should treat this
as closing, not deferring, the 14B question for this hardware tier absent a
larger-sample re-measurement someone judges worth the GPU time.

**Stack restored.** After the 14B draws completed, the standalone container
was removed, `ollama`/`llama-server-embed`/`llama-server` were restarted
(compose-managed `llama-server`/`llama-server-embed` containers were never
recreated — same container, same image digest, same `Cmd`, confirmed via
`docker inspect` before and after), and the agent container was restarted
and smoke-tested (`/health` → `200 {"status":"ok"}`, a real `/completion`
call succeeded) before this measurement was called done.

## The two engine-level levers that decide what fits

**q8_0 KV-cache quantization + flash-attn (llama.cpp).** This is the enabling configuration for the chosen model. Without KV-cache quantization, an 8B model's key/value cache at a 16k context window does not fit alongside the model weights inside 8 GB of VRAM; q8_0 quantizes the cache to roughly a quarter of its FP16 footprint, and flash-attn keeps the attention computation itself memory-efficient at that context length. Together they're what lets Qwen3-8B-Q5_K_M run **fully GPU-resident** at 6.63 GB / 8 GB — no CPU offload, no swap latency.

**MoE expert-offload (`--cpu-moe`).** This is the lever that makes a 30B-parameter model fit on an 8 GB card at all — only 1.9 GB of VRAM is used, with the mixture-of-experts layers living in CPU RAM and dispatched per-token. It's an elegant trick for the memory constraint, but it does not solve the latency constraint: every token that routes through a CPU-resident expert pays CPU memory-bandwidth cost, and that cost dominates at this GPU tier. Fitting in VRAM and being usable for an interactive query are two different bars, and the 30B-A3B configuration clears only the first one here.

## Why the ceiling on this hardware is low, and getting lower under the tightened definition

The exploratory sweep establishes a monotonic pattern up to the point where hardware fit breaks: 4B → 1/12, 8B-Q4 → 3/12, 8B-Q5 → 4/12 (exploratory, provenance-only). Larger models cite more reliably, model capacity is the binding constraint on reliable verbatim guideline citation, and 8B-Q5 is the largest model this hardware can hold fully GPU-resident at a usable context length. Stepping up further — to the 30B-A3B — does fit in VRAM via expert-offload, but at a latency (2.5–5 minutes per query, with a third of cases timing out entirely) that makes it unusable for interactive clinical use regardless of what its citation rate would be if every case ran to completion.

Tightening "verified" to require semantic support as well as provenance (issue #47/#81) accounts for exactly **one** of the 7 non-passing cases in the *historical* committed baseline (`statin-liver-monitoring-question`, downgraded from a provenance-only pass to `partially_verified` — see above). The other 6 non-passing cases reflect the same underlying capacity ceiling this document has documented from the start — an 8B model at this quantization does not reliably surface, quote, and correctly reason about retrieved guideline text in the same turn as chart-data tool calls — independent of the gate. Issue #100's live re-verification (above) found that ceiling applies even more broadly than the historical baseline showed: the 5 cases that baseline counted as passing turn out to reproduce that same chart-data-only / co-occurring-claim failure pattern on a fresh live draw, nearly every time.

That leaves a genuinely low number as the honest ceiling for this hardware tier under the tightened definition: not a bug in the verification layer, not a tuning gap in the prompt, but the direct consequence of (a) the largest model that fits an 8 GB card at interactive latency, now also required to (b) get the SEMANTICS of a citation right, not just its provenance. The fail-closed verifier and judge are doing their job correctly here — reporting the model's real reliability at this capacity, not padding the number with unverifiable or unsupported claims.

This is stated plainly rather than downplayed: the historical committed baseline reached **5 of 12** cases under the tightened bar — down from the earlier 6/12 provenance-only figure by exactly the one case the gate was built to catch — but issue #100's live re-verification found that number does not reproduce on a fresh live draw of those same 5 cases (1 `verified` outcome across 42 fresh draws total; see "Live re-verification (issue #100)" above). The honest, currently-reproducible number was **0 of 12** until issue #108's fix (see "Issue #108 follow-up" above): with the semantic-support judge's duplicate-claim inconsistency fixed, `a1c-target-question` and `lithium-nsaid-question` now reliably reach `verified` live, moving the honest number to **2 of 12**. **Separately, and disclosed rather than hidden:** a full fresh re-draw of the answer pipeline (not just the judge) is noisy on this 8B model — repeated from-scratch re-recording attempts have landed anywhere from 1/12 to 6/12 provenance-passing answers across draws, purely from planner/extraction variance; issue #100's finding is consistent with the low end of that already-disclosed range, now measured with much more data (42 draws) than a single re-roll. The 2/12 issue #108 unlocked does not contradict that ceiling discussion — it removed a verification-LAYER inconsistency bug, not a model-capacity limitation, from two cases that were already reliably surfacing and provenance-passing their guideline citation.

## Minimum vs. recommended tier

| | Minimum spec (measured, this doc) | Recommended spec |
|---|---|---|
| GPU | RTX 5060 Laptop, 8 GB VRAM | Larger single card with enough VRAM to hold a bigger model fully resident at the same context length |
| Model | Qwen3-8B-Q5_K_M, q8_0 KV, flash-attn | A larger dense or MoE model sized to the larger card, with experts (if MoE) also GPU-resident rather than CPU-offloaded |
| Verified citations (provenance AND semantic support) | 7/12 (measured, live-reproducible — see "Issue #133 findings" above; was 6/12 per "Issue #111/#128 findings", 4/12 per "Issue #114 findings", 3/12 per "Issue #116 follow-up", 2/12 per "Issue #108 follow-up", and 0/12 per "Live re-verification (issue #100)" before those fixes; the historical stable-recording baseline showed 5/12, but that did not survive a fresh live re-draw; fresh answer-pipeline re-draws vary 1/12–6/12, see the run-to-run variance caveat above) | Expected higher — not measured, since no recommended-tier hardware exists in this project; stated as directional only, per the same discipline `docs/W2_ARCHITECTURE.md` applies to its own projected numbers |
| Latency | ~27 s | Expected comparable or better, since the constraint driving the 30B-A3B's latency (CPU-offloaded experts) would not apply once a larger card holds the whole model in VRAM |

The mechanism is the same one `docs/W2_ARCHITECTURE.md` uses for the document-ingestion VLM: **model tier is a config value, not a code fork.** Swapping in a larger model at a larger-VRAM recommended tier changes citation reliability and latency; it does not change the verification layer's fail-closed contract. A clinician reading an answer on either tier gets the same guarantee — every surviving citation was both verbatim-verified against the guideline corpus AND judged to actually support the claim it's attached to — the two tiers differ only in how often the model succeeds at producing a citation that clears both bars in the first place.

## Summary

The chosen configuration — Qwen3-8B-Q5_K_M, 16k context, q8_0 KV-cache, flash-attn, fully GPU-resident on an 8 GB card — is the largest model this reference hardware can serve at interactive latency. Under the tightened definition of "verified" (provenance AND semantic support, issue #47/#81), a re-judge of the stable, already-committed answer recordings with a live semantic-support judge historically showed it clearing that bar on **5 of 12** guideline-citation eval cases — one case lower than the earlier 6/12 provenance-only figure, exactly the one case (`statin-liver-monitoring-question`) the gate was built to catch. **Issue #100 found that historical number does not hold up against a fresh live draw**: re-running all 5 of those cases live, repeatedly (42 draws total), reproduced `verified` only once. The honest, live-reproducible number for this hardware tier was **0 of 12** (see "Live re-verification (issue #100)" above) with all 12 `citation_present` cases documented `xfail` — until issue #108's fix (see "Issue #108 follow-up" above) resolved the semantic-support judge's duplicate-claim inconsistency, moving `a1c-target-question` and `lithium-nsaid-question` to a reliable, live-reproducible `verified` and the honest total to **2 of 12**, issue #116's stale-recording re-verification (see "Issue #116 follow-up" above) moved `warfarin-antibiotic-question` to the same standard, honest total to **3 of 12**, and issue #114's larger (25-draw) live sample (see "Issue #114 findings" above) found `statin-ck-myopathy-question`'s original chart-data-only pattern no longer reproduces (92% verified), moving the honest total to **4 of 12**; issues #111/#128's combined judge-context-scoping fix (see "Issue #111/#128 findings" above) then moved it to **6 of 12**, un-xfailing `bp-stage2-question` and `lipid-panel-ldl-question`; and issue #133's live re-verification (see "Issue #133 findings" above) found `nsaid-ace-inhibitor-question`'s old stale-recording rationale no longer reproduces, moving the honest total now **7 of 12** (`renal-function-ace-question` was checked at the same rigor and remains honestly `xfail`, for a distinct, freshly-documented semantic-support-judge finding, not a capacity or routing defect). This sharpens rather than reverses the document's core conclusion: the capacity ceiling on this hardware tier is real for the remaining cases, and it is lower than the historical baseline suggested; these 5 cases were never blocked by that capacity ceiling in the first place — they were blocked by verification-layer bugs or stale small-sample/stale-recording findings, now fixed/refreshed/re-measured. It is also the reason a recommended tier exists: a bigger GPU removes the capacity ceiling this document identifies, at the cost of hardware this project does not require to run.
